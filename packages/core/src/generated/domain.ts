/**
 * AUTO-GENERATED by scripts/generate-schema-types.mjs from every schema file
 * under schemas/. Do not hand-edit. Re-run: pnpm run generate:types
 */
/* eslint-disable */
// source: schemas/approval/approval-decision.schema.json
namespace NS_approval_approval_decision {
  /**
   * A UUID, RFC 9562. ACT logical identities MUST use UUIDv7 (time-ordered) when newly generated; this pattern accepts any RFC 9562 UUID so that imported/legacy identifiers are not rejected on shape alone.
   */
  export type UUID = string;
  /**
   * A content or event digest in the form algorithm:encoded-value. Version 1.0 requires sha-256.
   */
  export type Digest = string;

  /**
   * A signed decision bound to an exact subject id and digest. See ACT-1.0.md section 8.1.
   */
  export interface ApprovalDecision {
    decision_id: UUID;
    request_id: UUID;
    subject: {
      artifact_id: UUID;
      version_id: Digest;
    };
    decision: 'approved' | 'rejected' | 'changes_requested';
    scope: string;
    reviewer: ActorRef;
    reviewer_authority: {
      assignment_id: UUID;
      role: string;
    };
    policy_id: UUID;
    policy_version: Digest;
    conditions: string[];
    comments: string;
    issued_at: string;
    expires_at: string | null;
    supersedes: UUID | null;
    signature: {
      key_id: string;
      algorithm: 'ed25519';
      signature: string;
    };
  }
  /**
   * A reference to the actor and signing key responsible for an event or attestation.
   */
  export interface ActorRef {
    actor_id: UUID;
    /**
     * algorithm:fingerprint, e.g. ed25519:9f86d0...
     */
    key_id: string;
    display_name?: string;
  }
}

// source: schemas/approval/approval-request.schema.json
namespace NS_approval_approval_request {
  /**
   * A UUID, RFC 9562. ACT logical identities MUST use UUIDv7 (time-ordered) when newly generated; this pattern accepts any RFC 9562 UUID so that imported/legacy identifiers are not rejected on shape alone.
   */
  export type UUID = string;
  /**
   * A content or event digest in the form algorithm:encoded-value. Version 1.0 requires sha-256.
   */
  export type Digest = string;

  /**
   * A request for approval of an exact subject version. See ACT-1.0.md section 8.
   */
  export interface ApprovalRequest {
    request_id: UUID;
    subject: {
      artifact_id: UUID;
      version_id: Digest;
    };
    requested_by: ActorRef;
    policy_id: UUID;
    policy_version: Digest;
    requested_at: string;
    status: 'requested' | 'approved' | 'rejected' | 'changes_requested' | 'cancelled';
    /**
     * @minItems 1
     */
    signatures: [
      {
        key_id: string;
        algorithm: 'ed25519';
        signature: string;
      },
      ...{
        key_id: string;
        algorithm: 'ed25519';
        signature: string;
      }[],
    ];
  }
  /**
   * A reference to the actor and signing key responsible for an event or attestation.
   */
  export interface ActorRef {
    actor_id: UUID;
    /**
     * algorithm:fingerprint, e.g. ed25519:9f86d0...
     */
    key_id: string;
    display_name?: string;
  }
}

// source: schemas/artifact/artifact-envelope.schema.json
namespace NS_artifact_artifact_envelope {
  /**
   * Logical artifact identity, stable across revisions.
   */
  export type UUID = string;
  /**
   * Immutable version identity, the digest of this record's canonical bytes excluding signatures.
   */
  export type Digest = string;
  /**
   * A UUID, RFC 9562. ACT logical identities MUST use UUIDv7 (time-ordered) when newly generated; this pattern accepts any RFC 9562 UUID so that imported/legacy identifiers are not rejected on shape alone.
   */
  export type UUID1 = string;
  /**
   * A content or event digest in the form algorithm:encoded-value. Version 1.0 requires sha-256.
   */
  export type Digest1 = string;

  /**
   * The shared envelope every artifact-version payload extends, per ACT-1.0.md section 7.1 and PROMPT.md's Artifact Types requirements. Concrete type schemas under schemas/artifact/types/ set artifact_type to a fixed const and add a type-specific `data` object.
   */
  export interface ArtifactEnvelope {
    artifact_id: UUID;
    version_id: Digest;
    artifact_type: string;
    schema_version: string;
    protocol_version: 'act/1.0';
    authoring_actor: ActorRef;
    created_at_claim: string;
    content: ContentDescriptor;
    /**
     * Typed, many-to-many edges from this version to prior nodes it depends on. Empty only for a Genesis version.
     */
    lineage: {
      relation:
        'revises' | 'input-of' | 'output-of' | 'merges' | 'responds-to' | 'external-import-of';
      target_version_id: Digest1;
    }[];
    applicable_policy:
      | {
          policy_id: UUID1;
          policy_version: Digest1;
        }
      | {
          not_applicable: true;
          reason: string;
        };
    confidence_assessments: ConfidenceAssessment[];
    uncertainties: Uncertainty[];
    evidence_refs: Digest1[];
    /**
     * @minItems 1
     */
    signatures: [
      {
        key_id: string;
        algorithm: 'ed25519';
        signature: string;
      },
      ...{
        key_id: string;
        algorithm: 'ed25519';
        signature: string;
      }[],
    ];
    sensitivity: 'public' | 'internal' | 'confidential' | 'restricted';
    retention_policy_id: UUID1 | null;
    /**
     * Type-specific payload, defined by the concrete artifact type schema.
     */
    data: {};
    extensions?: {};
  }
  /**
   * A reference to the actor and signing key responsible for an event or attestation.
   */
  export interface ActorRef {
    actor_id: UUID1;
    /**
     * algorithm:fingerprint, e.g. ed25519:9f86d0...
     */
    key_id: string;
    display_name?: string;
  }
  /**
   * Describes artifact content separately from immutable event metadata, per ACT-1.0.md section 15.
   */
  export interface ContentDescriptor {
    media_type: string;
    byte_length: number;
    digest: Digest1;
    storage: {
      kind: 'inline' | 'filesystem' | 'database' | 'external';
      /**
       * base64-encoded content, present only when kind is inline
       */
      inline_value?: string;
      /**
       * opaque storage-provider locator, present when kind is filesystem/database/external
       */
      location?: string;
    };
    encryption?: {
      algorithm: 'none' | 'aes-256-gcm';
      key_id?: string;
      /**
       * base64-encoded nonce, required when algorithm is not none
       */
      nonce?: string;
    };
    sensitivity: 'public' | 'internal' | 'confidential' | 'restricted';
    retention_policy_id?: UUID1;
    availability_state: 'available' | 'redacted' | 'erased' | 'unavailable';
  }
  /**
   * A single-dimension, attributed confidence assessment, per ACT-1.0.md section 9.
   */
  export interface ConfidenceAssessment {
    dimension:
      | 'semantic'
      | 'requirement'
      | 'architectural'
      | 'implementation'
      | 'verification'
      | 'runtime'
      | 'source';
    score: number | 'unassessed';
    assessor: ActorRef;
    method: string;
    method_version: string;
    evidence: Digest1[];
    calibration_context?: string;
    timestamp: string;
    rationale: string;
  }
  /**
   * An uncertainty record, per ACT-1.0.md section 10.
   */
  export interface Uncertainty {
    id: UUID1;
    description: string;
    category:
      'known-unknown' | 'assumption' | 'speculation' | 'residual-risk' | 'human-input-required';
    source: ActorRef;
    introducing_transformation?: Digest1;
    /**
     * @minItems 1
     */
    affected_artifacts: [Digest1, ...Digest1[]];
    impact: 'low' | 'medium' | 'high' | 'critical';
    likelihood: 'low' | 'medium' | 'high';
    owner: ActorRef;
    status: 'open' | 'discharged' | 'accepted';
    resolution_criteria: string;
    inherited_from: UUID1[];
    resolving_evidence_or_decision?: Digest1 | null;
  }
}

// source: schemas/artifact/transformation.schema.json
namespace NS_artifact_transformation {
  /**
   * A content or event digest in the form algorithm:encoded-value. Version 1.0 requires sha-256.
   */
  export type Digest = string;
  /**
   * A UUID, RFC 9562. ACT logical identities MUST use UUIDv7 (time-ordered) when newly generated; this pattern accepts any RFC 9562 UUID so that imported/legacy identifiers are not rejected on shape alone.
   */
  export type UUID = string;

  /**
   * The transformation contract, per ACT-1.0.md section 7.2.
   */
  export interface Transformation {
    transformation_id: Digest;
    mode: 'preservation' | 'discovery';
    actor: ActorRef;
    /**
     * @minItems 1
     */
    inputs: [Digest, ...Digest[]];
    /**
     * @minItems 1
     */
    outputs: [Digest, ...Digest[]];
    semantic_change_claim: {
      classification:
        | 'exact-preservation'
        | 'clarification'
        | 'constraint-refinement'
        | 'assumption-introduction'
        | 'alternative-proposal'
        | 'intent-challenge'
        | 'semantic-modification';
      assessor: ActorRef;
      method?: string;
      equivalence_procedure?:
        | 'byte-identity'
        | 'canonical-structural-equality'
        | 'reproducible-build'
        | 'formal-proof'
        | 'not_applicable';
      confidence?: ConfidenceAssessment;
      dispute_status?: 'undisputed' | 'disputed' | 'resolved';
    };
    assumptions: string[];
    ambiguities: string[];
    alternatives: string[];
    rationale: string;
    confidence_assessments: ConfidenceAssessment[];
    uncertainties: Uncertainty[];
    evidence: Digest[];
    verification_results: Digest[];
    applicable_policy:
      | {
          policy_id: UUID;
          policy_version: Digest;
        }
      | {
          not_applicable: true;
          reason: string;
        };
    /**
     * The result of evaluating applicable_policy against semantic_change_claim.classification and subject. This is a policy-evaluation snapshot recorded for audit, not a mutable authority field.
     */
    approval_requirement: {
      required: boolean;
      reason: string;
      satisfied_by?: Digest[];
    };
  }
  /**
   * A reference to the actor and signing key responsible for an event or attestation.
   */
  export interface ActorRef {
    actor_id: UUID;
    /**
     * algorithm:fingerprint, e.g. ed25519:9f86d0...
     */
    key_id: string;
    display_name?: string;
  }
  /**
   * A single-dimension, attributed confidence assessment, per ACT-1.0.md section 9.
   */
  export interface ConfidenceAssessment {
    dimension:
      | 'semantic'
      | 'requirement'
      | 'architectural'
      | 'implementation'
      | 'verification'
      | 'runtime'
      | 'source';
    score: number | 'unassessed';
    assessor: ActorRef;
    method: string;
    method_version: string;
    evidence: Digest[];
    calibration_context?: string;
    timestamp: string;
    rationale: string;
  }
  /**
   * An uncertainty record, per ACT-1.0.md section 10.
   */
  export interface Uncertainty {
    id: UUID;
    description: string;
    category:
      'known-unknown' | 'assumption' | 'speculation' | 'residual-risk' | 'human-input-required';
    source: ActorRef;
    introducing_transformation?: Digest;
    /**
     * @minItems 1
     */
    affected_artifacts: [Digest, ...Digest[]];
    impact: 'low' | 'medium' | 'high' | 'critical';
    likelihood: 'low' | 'medium' | 'high';
    owner: ActorRef;
    status: 'open' | 'discharged' | 'accepted';
    resolution_criteria: string;
    inherited_from: UUID[];
    resolving_evidence_or_decision?: Digest | null;
  }
}

// source: schemas/artifact/types/accountability-assignment.schema.json
namespace NS_artifact_types_accountability_assignment {
  /**
   * A versioned assignment of a role to an actor over a scope and time window (ACT-1.0.md section 8.4).
   */
  export type AccountabilityAssignment = ArtifactEnvelope & {
    artifact_type: 'AccountabilityAssignment';
    data: {
      role:
        | 'proposer'
        | 'author'
        | 'reviewer'
        | 'approver'
        | 'executor'
        | 'operator'
        | 'owner'
        | 'incident_owner';
      assignee: ActorRef;
      scope: string;
      issuer: ActorRef;
      authority: string;
      start_time: string;
      end_time?: string | null;
      status: 'active' | 'ended' | 'revoked';
    };
  };
  /**
   * Logical artifact identity, stable across revisions.
   */
  export type UUID = string;
  /**
   * Immutable version identity, the digest of this record's canonical bytes excluding signatures.
   */
  export type Digest = string;
  /**
   * A UUID, RFC 9562. ACT logical identities MUST use UUIDv7 (time-ordered) when newly generated; this pattern accepts any RFC 9562 UUID so that imported/legacy identifiers are not rejected on shape alone.
   */
  export type UUID1 = string;
  /**
   * A content or event digest in the form algorithm:encoded-value. Version 1.0 requires sha-256.
   */
  export type Digest1 = string;

  /**
   * The shared envelope every artifact-version payload extends, per ACT-1.0.md section 7.1 and PROMPT.md's Artifact Types requirements. Concrete type schemas under schemas/artifact/types/ set artifact_type to a fixed const and add a type-specific `data` object.
   */
  export interface ArtifactEnvelope {
    artifact_id: UUID;
    version_id: Digest;
    artifact_type: string;
    schema_version: string;
    protocol_version: 'act/1.0';
    authoring_actor: ActorRef;
    created_at_claim: string;
    content: ContentDescriptor;
    /**
     * Typed, many-to-many edges from this version to prior nodes it depends on. Empty only for a Genesis version.
     */
    lineage: {
      relation:
        'revises' | 'input-of' | 'output-of' | 'merges' | 'responds-to' | 'external-import-of';
      target_version_id: Digest1;
    }[];
    applicable_policy:
      | {
          policy_id: UUID1;
          policy_version: Digest1;
        }
      | {
          not_applicable: true;
          reason: string;
        };
    confidence_assessments: ConfidenceAssessment[];
    uncertainties: Uncertainty[];
    evidence_refs: Digest1[];
    /**
     * @minItems 1
     */
    signatures: [
      {
        key_id: string;
        algorithm: 'ed25519';
        signature: string;
      },
      ...{
        key_id: string;
        algorithm: 'ed25519';
        signature: string;
      }[],
    ];
    sensitivity: 'public' | 'internal' | 'confidential' | 'restricted';
    retention_policy_id: UUID1 | null;
    /**
     * Type-specific payload, defined by the concrete artifact type schema.
     */
    data: {};
    extensions?: {};
  }
  /**
   * A reference to the actor and signing key responsible for an event or attestation.
   */
  export interface ActorRef {
    actor_id: UUID1;
    /**
     * algorithm:fingerprint, e.g. ed25519:9f86d0...
     */
    key_id: string;
    display_name?: string;
  }
  /**
   * Describes artifact content separately from immutable event metadata, per ACT-1.0.md section 15.
   */
  export interface ContentDescriptor {
    media_type: string;
    byte_length: number;
    digest: Digest1;
    storage: {
      kind: 'inline' | 'filesystem' | 'database' | 'external';
      /**
       * base64-encoded content, present only when kind is inline
       */
      inline_value?: string;
      /**
       * opaque storage-provider locator, present when kind is filesystem/database/external
       */
      location?: string;
    };
    encryption?: {
      algorithm: 'none' | 'aes-256-gcm';
      key_id?: string;
      /**
       * base64-encoded nonce, required when algorithm is not none
       */
      nonce?: string;
    };
    sensitivity: 'public' | 'internal' | 'confidential' | 'restricted';
    retention_policy_id?: UUID1;
    availability_state: 'available' | 'redacted' | 'erased' | 'unavailable';
  }
  /**
   * A single-dimension, attributed confidence assessment, per ACT-1.0.md section 9.
   */
  export interface ConfidenceAssessment {
    dimension:
      | 'semantic'
      | 'requirement'
      | 'architectural'
      | 'implementation'
      | 'verification'
      | 'runtime'
      | 'source';
    score: number | 'unassessed';
    assessor: ActorRef;
    method: string;
    method_version: string;
    evidence: Digest1[];
    calibration_context?: string;
    timestamp: string;
    rationale: string;
  }
  /**
   * An uncertainty record, per ACT-1.0.md section 10.
   */
  export interface Uncertainty {
    id: UUID1;
    description: string;
    category:
      'known-unknown' | 'assumption' | 'speculation' | 'residual-risk' | 'human-input-required';
    source: ActorRef;
    introducing_transformation?: Digest1;
    /**
     * @minItems 1
     */
    affected_artifacts: [Digest1, ...Digest1[]];
    impact: 'low' | 'medium' | 'high' | 'critical';
    likelihood: 'low' | 'medium' | 'high';
    owner: ActorRef;
    status: 'open' | 'discharged' | 'accepted';
    resolution_criteria: string;
    inherited_from: UUID1[];
    resolving_evidence_or_decision?: Digest1 | null;
  }
}

// source: schemas/artifact/types/actor.schema.json
namespace NS_artifact_types_actor {
  /**
   * A registered protocol actor.
   */
  export type Actor = ArtifactEnvelope & {
    artifact_type: 'Actor';
    data: {
      actor_type: 'human' | 'ai-system' | 'service' | 'organization' | 'group';
      display_name: string;
      keys: string[];
    };
  };
  /**
   * Logical artifact identity, stable across revisions.
   */
  export type UUID = string;
  /**
   * Immutable version identity, the digest of this record's canonical bytes excluding signatures.
   */
  export type Digest = string;
  /**
   * A UUID, RFC 9562. ACT logical identities MUST use UUIDv7 (time-ordered) when newly generated; this pattern accepts any RFC 9562 UUID so that imported/legacy identifiers are not rejected on shape alone.
   */
  export type UUID1 = string;
  /**
   * A content or event digest in the form algorithm:encoded-value. Version 1.0 requires sha-256.
   */
  export type Digest1 = string;

  /**
   * The shared envelope every artifact-version payload extends, per ACT-1.0.md section 7.1 and PROMPT.md's Artifact Types requirements. Concrete type schemas under schemas/artifact/types/ set artifact_type to a fixed const and add a type-specific `data` object.
   */
  export interface ArtifactEnvelope {
    artifact_id: UUID;
    version_id: Digest;
    artifact_type: string;
    schema_version: string;
    protocol_version: 'act/1.0';
    authoring_actor: ActorRef;
    created_at_claim: string;
    content: ContentDescriptor;
    /**
     * Typed, many-to-many edges from this version to prior nodes it depends on. Empty only for a Genesis version.
     */
    lineage: {
      relation:
        'revises' | 'input-of' | 'output-of' | 'merges' | 'responds-to' | 'external-import-of';
      target_version_id: Digest1;
    }[];
    applicable_policy:
      | {
          policy_id: UUID1;
          policy_version: Digest1;
        }
      | {
          not_applicable: true;
          reason: string;
        };
    confidence_assessments: ConfidenceAssessment[];
    uncertainties: Uncertainty[];
    evidence_refs: Digest1[];
    /**
     * @minItems 1
     */
    signatures: [
      {
        key_id: string;
        algorithm: 'ed25519';
        signature: string;
      },
      ...{
        key_id: string;
        algorithm: 'ed25519';
        signature: string;
      }[],
    ];
    sensitivity: 'public' | 'internal' | 'confidential' | 'restricted';
    retention_policy_id: UUID1 | null;
    /**
     * Type-specific payload, defined by the concrete artifact type schema.
     */
    data: {};
    extensions?: {};
  }
  /**
   * A reference to the actor and signing key responsible for an event or attestation.
   */
  export interface ActorRef {
    actor_id: UUID1;
    /**
     * algorithm:fingerprint, e.g. ed25519:9f86d0...
     */
    key_id: string;
    display_name?: string;
  }
  /**
   * Describes artifact content separately from immutable event metadata, per ACT-1.0.md section 15.
   */
  export interface ContentDescriptor {
    media_type: string;
    byte_length: number;
    digest: Digest1;
    storage: {
      kind: 'inline' | 'filesystem' | 'database' | 'external';
      /**
       * base64-encoded content, present only when kind is inline
       */
      inline_value?: string;
      /**
       * opaque storage-provider locator, present when kind is filesystem/database/external
       */
      location?: string;
    };
    encryption?: {
      algorithm: 'none' | 'aes-256-gcm';
      key_id?: string;
      /**
       * base64-encoded nonce, required when algorithm is not none
       */
      nonce?: string;
    };
    sensitivity: 'public' | 'internal' | 'confidential' | 'restricted';
    retention_policy_id?: UUID1;
    availability_state: 'available' | 'redacted' | 'erased' | 'unavailable';
  }
  /**
   * A single-dimension, attributed confidence assessment, per ACT-1.0.md section 9.
   */
  export interface ConfidenceAssessment {
    dimension:
      | 'semantic'
      | 'requirement'
      | 'architectural'
      | 'implementation'
      | 'verification'
      | 'runtime'
      | 'source';
    score: number | 'unassessed';
    assessor: ActorRef;
    method: string;
    method_version: string;
    evidence: Digest1[];
    calibration_context?: string;
    timestamp: string;
    rationale: string;
  }
  /**
   * An uncertainty record, per ACT-1.0.md section 10.
   */
  export interface Uncertainty {
    id: UUID1;
    description: string;
    category:
      'known-unknown' | 'assumption' | 'speculation' | 'residual-risk' | 'human-input-required';
    source: ActorRef;
    introducing_transformation?: Digest1;
    /**
     * @minItems 1
     */
    affected_artifacts: [Digest1, ...Digest1[]];
    impact: 'low' | 'medium' | 'high' | 'critical';
    likelihood: 'low' | 'medium' | 'high';
    owner: ActorRef;
    status: 'open' | 'discharged' | 'accepted';
    resolution_criteria: string;
    inherited_from: UUID1[];
    resolving_evidence_or_decision?: Digest1 | null;
  }
}

// source: schemas/artifact/types/ai-proposal.schema.json
namespace NS_artifact_types_ai_proposal {
  /**
   * A proposal authored by an AI system.
   */
  export type AIProposal = ArtifactEnvelope & {
    artifact_type: 'AIProposal';
    data: {
      proposer: ActorRef;
      model: string;
      model_version?: string;
      content_digest: Digest1;
      rationale: string;
    };
  };
  /**
   * Logical artifact identity, stable across revisions.
   */
  export type UUID = string;
  /**
   * Immutable version identity, the digest of this record's canonical bytes excluding signatures.
   */
  export type Digest = string;
  /**
   * A UUID, RFC 9562. ACT logical identities MUST use UUIDv7 (time-ordered) when newly generated; this pattern accepts any RFC 9562 UUID so that imported/legacy identifiers are not rejected on shape alone.
   */
  export type UUID1 = string;
  /**
   * A content or event digest in the form algorithm:encoded-value. Version 1.0 requires sha-256.
   */
  export type Digest1 = string;

  /**
   * The shared envelope every artifact-version payload extends, per ACT-1.0.md section 7.1 and PROMPT.md's Artifact Types requirements. Concrete type schemas under schemas/artifact/types/ set artifact_type to a fixed const and add a type-specific `data` object.
   */
  export interface ArtifactEnvelope {
    artifact_id: UUID;
    version_id: Digest;
    artifact_type: string;
    schema_version: string;
    protocol_version: 'act/1.0';
    authoring_actor: ActorRef;
    created_at_claim: string;
    content: ContentDescriptor;
    /**
     * Typed, many-to-many edges from this version to prior nodes it depends on. Empty only for a Genesis version.
     */
    lineage: {
      relation:
        'revises' | 'input-of' | 'output-of' | 'merges' | 'responds-to' | 'external-import-of';
      target_version_id: Digest1;
    }[];
    applicable_policy:
      | {
          policy_id: UUID1;
          policy_version: Digest1;
        }
      | {
          not_applicable: true;
          reason: string;
        };
    confidence_assessments: ConfidenceAssessment[];
    uncertainties: Uncertainty[];
    evidence_refs: Digest1[];
    /**
     * @minItems 1
     */
    signatures: [
      {
        key_id: string;
        algorithm: 'ed25519';
        signature: string;
      },
      ...{
        key_id: string;
        algorithm: 'ed25519';
        signature: string;
      }[],
    ];
    sensitivity: 'public' | 'internal' | 'confidential' | 'restricted';
    retention_policy_id: UUID1 | null;
    /**
     * Type-specific payload, defined by the concrete artifact type schema.
     */
    data: {};
    extensions?: {};
  }
  /**
   * A reference to the actor and signing key responsible for an event or attestation.
   */
  export interface ActorRef {
    actor_id: UUID1;
    /**
     * algorithm:fingerprint, e.g. ed25519:9f86d0...
     */
    key_id: string;
    display_name?: string;
  }
  /**
   * Describes artifact content separately from immutable event metadata, per ACT-1.0.md section 15.
   */
  export interface ContentDescriptor {
    media_type: string;
    byte_length: number;
    digest: Digest1;
    storage: {
      kind: 'inline' | 'filesystem' | 'database' | 'external';
      /**
       * base64-encoded content, present only when kind is inline
       */
      inline_value?: string;
      /**
       * opaque storage-provider locator, present when kind is filesystem/database/external
       */
      location?: string;
    };
    encryption?: {
      algorithm: 'none' | 'aes-256-gcm';
      key_id?: string;
      /**
       * base64-encoded nonce, required when algorithm is not none
       */
      nonce?: string;
    };
    sensitivity: 'public' | 'internal' | 'confidential' | 'restricted';
    retention_policy_id?: UUID1;
    availability_state: 'available' | 'redacted' | 'erased' | 'unavailable';
  }
  /**
   * A single-dimension, attributed confidence assessment, per ACT-1.0.md section 9.
   */
  export interface ConfidenceAssessment {
    dimension:
      | 'semantic'
      | 'requirement'
      | 'architectural'
      | 'implementation'
      | 'verification'
      | 'runtime'
      | 'source';
    score: number | 'unassessed';
    assessor: ActorRef;
    method: string;
    method_version: string;
    evidence: Digest1[];
    calibration_context?: string;
    timestamp: string;
    rationale: string;
  }
  /**
   * An uncertainty record, per ACT-1.0.md section 10.
   */
  export interface Uncertainty {
    id: UUID1;
    description: string;
    category:
      'known-unknown' | 'assumption' | 'speculation' | 'residual-risk' | 'human-input-required';
    source: ActorRef;
    introducing_transformation?: Digest1;
    /**
     * @minItems 1
     */
    affected_artifacts: [Digest1, ...Digest1[]];
    impact: 'low' | 'medium' | 'high' | 'critical';
    likelihood: 'low' | 'medium' | 'high';
    owner: ActorRef;
    status: 'open' | 'discharged' | 'accepted';
    resolution_criteria: string;
    inherited_from: UUID1[];
    resolving_evidence_or_decision?: Digest1 | null;
  }
}

// source: schemas/artifact/types/ambiguity.schema.json
namespace NS_artifact_types_ambiguity {
  /**
   * A recorded point where meaning is not yet settled.
   */
  export type Ambiguity = ArtifactEnvelope & {
    artifact_type: 'Ambiguity';
    data: {
      description: string;
      /**
       * @minItems 1
       */
      candidate_interpretations: [string, ...string[]];
      resolved: boolean;
    };
  };
  /**
   * Logical artifact identity, stable across revisions.
   */
  export type UUID = string;
  /**
   * Immutable version identity, the digest of this record's canonical bytes excluding signatures.
   */
  export type Digest = string;
  /**
   * A UUID, RFC 9562. ACT logical identities MUST use UUIDv7 (time-ordered) when newly generated; this pattern accepts any RFC 9562 UUID so that imported/legacy identifiers are not rejected on shape alone.
   */
  export type UUID1 = string;
  /**
   * A content or event digest in the form algorithm:encoded-value. Version 1.0 requires sha-256.
   */
  export type Digest1 = string;

  /**
   * The shared envelope every artifact-version payload extends, per ACT-1.0.md section 7.1 and PROMPT.md's Artifact Types requirements. Concrete type schemas under schemas/artifact/types/ set artifact_type to a fixed const and add a type-specific `data` object.
   */
  export interface ArtifactEnvelope {
    artifact_id: UUID;
    version_id: Digest;
    artifact_type: string;
    schema_version: string;
    protocol_version: 'act/1.0';
    authoring_actor: ActorRef;
    created_at_claim: string;
    content: ContentDescriptor;
    /**
     * Typed, many-to-many edges from this version to prior nodes it depends on. Empty only for a Genesis version.
     */
    lineage: {
      relation:
        'revises' | 'input-of' | 'output-of' | 'merges' | 'responds-to' | 'external-import-of';
      target_version_id: Digest1;
    }[];
    applicable_policy:
      | {
          policy_id: UUID1;
          policy_version: Digest1;
        }
      | {
          not_applicable: true;
          reason: string;
        };
    confidence_assessments: ConfidenceAssessment[];
    uncertainties: Uncertainty[];
    evidence_refs: Digest1[];
    /**
     * @minItems 1
     */
    signatures: [
      {
        key_id: string;
        algorithm: 'ed25519';
        signature: string;
      },
      ...{
        key_id: string;
        algorithm: 'ed25519';
        signature: string;
      }[],
    ];
    sensitivity: 'public' | 'internal' | 'confidential' | 'restricted';
    retention_policy_id: UUID1 | null;
    /**
     * Type-specific payload, defined by the concrete artifact type schema.
     */
    data: {};
    extensions?: {};
  }
  /**
   * A reference to the actor and signing key responsible for an event or attestation.
   */
  export interface ActorRef {
    actor_id: UUID1;
    /**
     * algorithm:fingerprint, e.g. ed25519:9f86d0...
     */
    key_id: string;
    display_name?: string;
  }
  /**
   * Describes artifact content separately from immutable event metadata, per ACT-1.0.md section 15.
   */
  export interface ContentDescriptor {
    media_type: string;
    byte_length: number;
    digest: Digest1;
    storage: {
      kind: 'inline' | 'filesystem' | 'database' | 'external';
      /**
       * base64-encoded content, present only when kind is inline
       */
      inline_value?: string;
      /**
       * opaque storage-provider locator, present when kind is filesystem/database/external
       */
      location?: string;
    };
    encryption?: {
      algorithm: 'none' | 'aes-256-gcm';
      key_id?: string;
      /**
       * base64-encoded nonce, required when algorithm is not none
       */
      nonce?: string;
    };
    sensitivity: 'public' | 'internal' | 'confidential' | 'restricted';
    retention_policy_id?: UUID1;
    availability_state: 'available' | 'redacted' | 'erased' | 'unavailable';
  }
  /**
   * A single-dimension, attributed confidence assessment, per ACT-1.0.md section 9.
   */
  export interface ConfidenceAssessment {
    dimension:
      | 'semantic'
      | 'requirement'
      | 'architectural'
      | 'implementation'
      | 'verification'
      | 'runtime'
      | 'source';
    score: number | 'unassessed';
    assessor: ActorRef;
    method: string;
    method_version: string;
    evidence: Digest1[];
    calibration_context?: string;
    timestamp: string;
    rationale: string;
  }
  /**
   * An uncertainty record, per ACT-1.0.md section 10.
   */
  export interface Uncertainty {
    id: UUID1;
    description: string;
    category:
      'known-unknown' | 'assumption' | 'speculation' | 'residual-risk' | 'human-input-required';
    source: ActorRef;
    introducing_transformation?: Digest1;
    /**
     * @minItems 1
     */
    affected_artifacts: [Digest1, ...Digest1[]];
    impact: 'low' | 'medium' | 'high' | 'critical';
    likelihood: 'low' | 'medium' | 'high';
    owner: ActorRef;
    status: 'open' | 'discharged' | 'accepted';
    resolution_criteria: string;
    inherited_from: UUID1[];
    resolving_evidence_or_decision?: Digest1 | null;
  }
}

// source: schemas/artifact/types/approval-decision.schema.json
namespace NS_artifact_types_approval_decision {
  /**
   * Wraps schemas/approval/approval-decision.schema.json as a lineage-tracked artifact.
   */
  export type ApprovalDecisionArtifact = ArtifactEnvelope & {
    artifact_type: 'ApprovalDecision';
    data: {
      decision: ApprovalDecision;
    };
  };
  /**
   * Logical artifact identity, stable across revisions.
   */
  export type UUID = string;
  /**
   * Immutable version identity, the digest of this record's canonical bytes excluding signatures.
   */
  export type Digest = string;
  /**
   * A UUID, RFC 9562. ACT logical identities MUST use UUIDv7 (time-ordered) when newly generated; this pattern accepts any RFC 9562 UUID so that imported/legacy identifiers are not rejected on shape alone.
   */
  export type UUID1 = string;
  /**
   * A content or event digest in the form algorithm:encoded-value. Version 1.0 requires sha-256.
   */
  export type Digest1 = string;

  /**
   * The shared envelope every artifact-version payload extends, per ACT-1.0.md section 7.1 and PROMPT.md's Artifact Types requirements. Concrete type schemas under schemas/artifact/types/ set artifact_type to a fixed const and add a type-specific `data` object.
   */
  export interface ArtifactEnvelope {
    artifact_id: UUID;
    version_id: Digest;
    artifact_type: string;
    schema_version: string;
    protocol_version: 'act/1.0';
    authoring_actor: ActorRef;
    created_at_claim: string;
    content: ContentDescriptor;
    /**
     * Typed, many-to-many edges from this version to prior nodes it depends on. Empty only for a Genesis version.
     */
    lineage: {
      relation:
        'revises' | 'input-of' | 'output-of' | 'merges' | 'responds-to' | 'external-import-of';
      target_version_id: Digest1;
    }[];
    applicable_policy:
      | {
          policy_id: UUID1;
          policy_version: Digest1;
        }
      | {
          not_applicable: true;
          reason: string;
        };
    confidence_assessments: ConfidenceAssessment[];
    uncertainties: Uncertainty[];
    evidence_refs: Digest1[];
    /**
     * @minItems 1
     */
    signatures: [
      {
        key_id: string;
        algorithm: 'ed25519';
        signature: string;
      },
      ...{
        key_id: string;
        algorithm: 'ed25519';
        signature: string;
      }[],
    ];
    sensitivity: 'public' | 'internal' | 'confidential' | 'restricted';
    retention_policy_id: UUID1 | null;
    /**
     * Type-specific payload, defined by the concrete artifact type schema.
     */
    data: {};
    extensions?: {};
  }
  /**
   * A reference to the actor and signing key responsible for an event or attestation.
   */
  export interface ActorRef {
    actor_id: UUID1;
    /**
     * algorithm:fingerprint, e.g. ed25519:9f86d0...
     */
    key_id: string;
    display_name?: string;
  }
  /**
   * Describes artifact content separately from immutable event metadata, per ACT-1.0.md section 15.
   */
  export interface ContentDescriptor {
    media_type: string;
    byte_length: number;
    digest: Digest1;
    storage: {
      kind: 'inline' | 'filesystem' | 'database' | 'external';
      /**
       * base64-encoded content, present only when kind is inline
       */
      inline_value?: string;
      /**
       * opaque storage-provider locator, present when kind is filesystem/database/external
       */
      location?: string;
    };
    encryption?: {
      algorithm: 'none' | 'aes-256-gcm';
      key_id?: string;
      /**
       * base64-encoded nonce, required when algorithm is not none
       */
      nonce?: string;
    };
    sensitivity: 'public' | 'internal' | 'confidential' | 'restricted';
    retention_policy_id?: UUID1;
    availability_state: 'available' | 'redacted' | 'erased' | 'unavailable';
  }
  /**
   * A single-dimension, attributed confidence assessment, per ACT-1.0.md section 9.
   */
  export interface ConfidenceAssessment {
    dimension:
      | 'semantic'
      | 'requirement'
      | 'architectural'
      | 'implementation'
      | 'verification'
      | 'runtime'
      | 'source';
    score: number | 'unassessed';
    assessor: ActorRef;
    method: string;
    method_version: string;
    evidence: Digest1[];
    calibration_context?: string;
    timestamp: string;
    rationale: string;
  }
  /**
   * An uncertainty record, per ACT-1.0.md section 10.
   */
  export interface Uncertainty {
    id: UUID1;
    description: string;
    category:
      'known-unknown' | 'assumption' | 'speculation' | 'residual-risk' | 'human-input-required';
    source: ActorRef;
    introducing_transformation?: Digest1;
    /**
     * @minItems 1
     */
    affected_artifacts: [Digest1, ...Digest1[]];
    impact: 'low' | 'medium' | 'high' | 'critical';
    likelihood: 'low' | 'medium' | 'high';
    owner: ActorRef;
    status: 'open' | 'discharged' | 'accepted';
    resolution_criteria: string;
    inherited_from: UUID1[];
    resolving_evidence_or_decision?: Digest1 | null;
  }
  /**
   * A signed decision bound to an exact subject id and digest. See ACT-1.0.md section 8.1.
   */
  export interface ApprovalDecision {
    decision_id: UUID1;
    request_id: UUID1;
    subject: {
      artifact_id: UUID1;
      version_id: Digest1;
    };
    decision: 'approved' | 'rejected' | 'changes_requested';
    scope: string;
    reviewer: ActorRef;
    reviewer_authority: {
      assignment_id: UUID1;
      role: string;
    };
    policy_id: UUID1;
    policy_version: Digest1;
    conditions: string[];
    comments: string;
    issued_at: string;
    expires_at: string | null;
    supersedes: UUID1 | null;
    signature: {
      key_id: string;
      algorithm: 'ed25519';
      signature: string;
    };
  }
}

// source: schemas/artifact/types/approval-request.schema.json
namespace NS_artifact_types_approval_request {
  /**
   * Wraps schemas/approval/approval-request.schema.json as a lineage-tracked artifact.
   */
  export type ApprovalRequestArtifact = ArtifactEnvelope & {
    artifact_type: 'ApprovalRequest';
    data: {
      request: ApprovalRequest;
    };
  };
  /**
   * Logical artifact identity, stable across revisions.
   */
  export type UUID = string;
  /**
   * Immutable version identity, the digest of this record's canonical bytes excluding signatures.
   */
  export type Digest = string;
  /**
   * A UUID, RFC 9562. ACT logical identities MUST use UUIDv7 (time-ordered) when newly generated; this pattern accepts any RFC 9562 UUID so that imported/legacy identifiers are not rejected on shape alone.
   */
  export type UUID1 = string;
  /**
   * A content or event digest in the form algorithm:encoded-value. Version 1.0 requires sha-256.
   */
  export type Digest1 = string;

  /**
   * The shared envelope every artifact-version payload extends, per ACT-1.0.md section 7.1 and PROMPT.md's Artifact Types requirements. Concrete type schemas under schemas/artifact/types/ set artifact_type to a fixed const and add a type-specific `data` object.
   */
  export interface ArtifactEnvelope {
    artifact_id: UUID;
    version_id: Digest;
    artifact_type: string;
    schema_version: string;
    protocol_version: 'act/1.0';
    authoring_actor: ActorRef;
    created_at_claim: string;
    content: ContentDescriptor;
    /**
     * Typed, many-to-many edges from this version to prior nodes it depends on. Empty only for a Genesis version.
     */
    lineage: {
      relation:
        'revises' | 'input-of' | 'output-of' | 'merges' | 'responds-to' | 'external-import-of';
      target_version_id: Digest1;
    }[];
    applicable_policy:
      | {
          policy_id: UUID1;
          policy_version: Digest1;
        }
      | {
          not_applicable: true;
          reason: string;
        };
    confidence_assessments: ConfidenceAssessment[];
    uncertainties: Uncertainty[];
    evidence_refs: Digest1[];
    /**
     * @minItems 1
     */
    signatures: [
      {
        key_id: string;
        algorithm: 'ed25519';
        signature: string;
      },
      ...{
        key_id: string;
        algorithm: 'ed25519';
        signature: string;
      }[],
    ];
    sensitivity: 'public' | 'internal' | 'confidential' | 'restricted';
    retention_policy_id: UUID1 | null;
    /**
     * Type-specific payload, defined by the concrete artifact type schema.
     */
    data: {};
    extensions?: {};
  }
  /**
   * A reference to the actor and signing key responsible for an event or attestation.
   */
  export interface ActorRef {
    actor_id: UUID1;
    /**
     * algorithm:fingerprint, e.g. ed25519:9f86d0...
     */
    key_id: string;
    display_name?: string;
  }
  /**
   * Describes artifact content separately from immutable event metadata, per ACT-1.0.md section 15.
   */
  export interface ContentDescriptor {
    media_type: string;
    byte_length: number;
    digest: Digest1;
    storage: {
      kind: 'inline' | 'filesystem' | 'database' | 'external';
      /**
       * base64-encoded content, present only when kind is inline
       */
      inline_value?: string;
      /**
       * opaque storage-provider locator, present when kind is filesystem/database/external
       */
      location?: string;
    };
    encryption?: {
      algorithm: 'none' | 'aes-256-gcm';
      key_id?: string;
      /**
       * base64-encoded nonce, required when algorithm is not none
       */
      nonce?: string;
    };
    sensitivity: 'public' | 'internal' | 'confidential' | 'restricted';
    retention_policy_id?: UUID1;
    availability_state: 'available' | 'redacted' | 'erased' | 'unavailable';
  }
  /**
   * A single-dimension, attributed confidence assessment, per ACT-1.0.md section 9.
   */
  export interface ConfidenceAssessment {
    dimension:
      | 'semantic'
      | 'requirement'
      | 'architectural'
      | 'implementation'
      | 'verification'
      | 'runtime'
      | 'source';
    score: number | 'unassessed';
    assessor: ActorRef;
    method: string;
    method_version: string;
    evidence: Digest1[];
    calibration_context?: string;
    timestamp: string;
    rationale: string;
  }
  /**
   * An uncertainty record, per ACT-1.0.md section 10.
   */
  export interface Uncertainty {
    id: UUID1;
    description: string;
    category:
      'known-unknown' | 'assumption' | 'speculation' | 'residual-risk' | 'human-input-required';
    source: ActorRef;
    introducing_transformation?: Digest1;
    /**
     * @minItems 1
     */
    affected_artifacts: [Digest1, ...Digest1[]];
    impact: 'low' | 'medium' | 'high' | 'critical';
    likelihood: 'low' | 'medium' | 'high';
    owner: ActorRef;
    status: 'open' | 'discharged' | 'accepted';
    resolution_criteria: string;
    inherited_from: UUID1[];
    resolving_evidence_or_decision?: Digest1 | null;
  }
  /**
   * A request for approval of an exact subject version. See ACT-1.0.md section 8.
   */
  export interface ApprovalRequest {
    request_id: UUID1;
    subject: {
      artifact_id: UUID1;
      version_id: Digest1;
    };
    requested_by: ActorRef;
    policy_id: UUID1;
    policy_version: Digest1;
    requested_at: string;
    status: 'requested' | 'approved' | 'rejected' | 'changes_requested' | 'cancelled';
    /**
     * @minItems 1
     */
    signatures: [
      {
        key_id: string;
        algorithm: 'ed25519';
        signature: string;
      },
      ...{
        key_id: string;
        algorithm: 'ed25519';
        signature: string;
      }[],
    ];
  }
}

// source: schemas/artifact/types/architecture.schema.json
namespace NS_artifact_types_architecture {
  /**
   * A recorded architectural structure or decision surface.
   */
  export type Architecture = ArtifactEnvelope & {
    artifact_type: 'Architecture';
    data: {
      title: string;
      description: string;
      /**
       * @minItems 1
       */
      components: [string, ...string[]];
      diagram_ref?: Digest1;
    };
  };
  /**
   * Logical artifact identity, stable across revisions.
   */
  export type UUID = string;
  /**
   * Immutable version identity, the digest of this record's canonical bytes excluding signatures.
   */
  export type Digest = string;
  /**
   * A UUID, RFC 9562. ACT logical identities MUST use UUIDv7 (time-ordered) when newly generated; this pattern accepts any RFC 9562 UUID so that imported/legacy identifiers are not rejected on shape alone.
   */
  export type UUID1 = string;
  /**
   * A content or event digest in the form algorithm:encoded-value. Version 1.0 requires sha-256.
   */
  export type Digest1 = string;

  /**
   * The shared envelope every artifact-version payload extends, per ACT-1.0.md section 7.1 and PROMPT.md's Artifact Types requirements. Concrete type schemas under schemas/artifact/types/ set artifact_type to a fixed const and add a type-specific `data` object.
   */
  export interface ArtifactEnvelope {
    artifact_id: UUID;
    version_id: Digest;
    artifact_type: string;
    schema_version: string;
    protocol_version: 'act/1.0';
    authoring_actor: ActorRef;
    created_at_claim: string;
    content: ContentDescriptor;
    /**
     * Typed, many-to-many edges from this version to prior nodes it depends on. Empty only for a Genesis version.
     */
    lineage: {
      relation:
        'revises' | 'input-of' | 'output-of' | 'merges' | 'responds-to' | 'external-import-of';
      target_version_id: Digest1;
    }[];
    applicable_policy:
      | {
          policy_id: UUID1;
          policy_version: Digest1;
        }
      | {
          not_applicable: true;
          reason: string;
        };
    confidence_assessments: ConfidenceAssessment[];
    uncertainties: Uncertainty[];
    evidence_refs: Digest1[];
    /**
     * @minItems 1
     */
    signatures: [
      {
        key_id: string;
        algorithm: 'ed25519';
        signature: string;
      },
      ...{
        key_id: string;
        algorithm: 'ed25519';
        signature: string;
      }[],
    ];
    sensitivity: 'public' | 'internal' | 'confidential' | 'restricted';
    retention_policy_id: UUID1 | null;
    /**
     * Type-specific payload, defined by the concrete artifact type schema.
     */
    data: {};
    extensions?: {};
  }
  /**
   * A reference to the actor and signing key responsible for an event or attestation.
   */
  export interface ActorRef {
    actor_id: UUID1;
    /**
     * algorithm:fingerprint, e.g. ed25519:9f86d0...
     */
    key_id: string;
    display_name?: string;
  }
  /**
   * Describes artifact content separately from immutable event metadata, per ACT-1.0.md section 15.
   */
  export interface ContentDescriptor {
    media_type: string;
    byte_length: number;
    digest: Digest1;
    storage: {
      kind: 'inline' | 'filesystem' | 'database' | 'external';
      /**
       * base64-encoded content, present only when kind is inline
       */
      inline_value?: string;
      /**
       * opaque storage-provider locator, present when kind is filesystem/database/external
       */
      location?: string;
    };
    encryption?: {
      algorithm: 'none' | 'aes-256-gcm';
      key_id?: string;
      /**
       * base64-encoded nonce, required when algorithm is not none
       */
      nonce?: string;
    };
    sensitivity: 'public' | 'internal' | 'confidential' | 'restricted';
    retention_policy_id?: UUID1;
    availability_state: 'available' | 'redacted' | 'erased' | 'unavailable';
  }
  /**
   * A single-dimension, attributed confidence assessment, per ACT-1.0.md section 9.
   */
  export interface ConfidenceAssessment {
    dimension:
      | 'semantic'
      | 'requirement'
      | 'architectural'
      | 'implementation'
      | 'verification'
      | 'runtime'
      | 'source';
    score: number | 'unassessed';
    assessor: ActorRef;
    method: string;
    method_version: string;
    evidence: Digest1[];
    calibration_context?: string;
    timestamp: string;
    rationale: string;
  }
  /**
   * An uncertainty record, per ACT-1.0.md section 10.
   */
  export interface Uncertainty {
    id: UUID1;
    description: string;
    category:
      'known-unknown' | 'assumption' | 'speculation' | 'residual-risk' | 'human-input-required';
    source: ActorRef;
    introducing_transformation?: Digest1;
    /**
     * @minItems 1
     */
    affected_artifacts: [Digest1, ...Digest1[]];
    impact: 'low' | 'medium' | 'high' | 'critical';
    likelihood: 'low' | 'medium' | 'high';
    owner: ActorRef;
    status: 'open' | 'discharged' | 'accepted';
    resolution_criteria: string;
    inherited_from: UUID1[];
    resolving_evidence_or_decision?: Digest1 | null;
  }
}

// source: schemas/artifact/types/assumption.schema.json
namespace NS_artifact_types_assumption {
  /**
   * A stated belief taken as true without full verification.
   */
  export type Assumption = ArtifactEnvelope & {
    artifact_type: 'Assumption';
    data: {
      statement: string;
      confidence_basis?: string;
      validated: boolean;
    };
  };
  /**
   * Logical artifact identity, stable across revisions.
   */
  export type UUID = string;
  /**
   * Immutable version identity, the digest of this record's canonical bytes excluding signatures.
   */
  export type Digest = string;
  /**
   * A UUID, RFC 9562. ACT logical identities MUST use UUIDv7 (time-ordered) when newly generated; this pattern accepts any RFC 9562 UUID so that imported/legacy identifiers are not rejected on shape alone.
   */
  export type UUID1 = string;
  /**
   * A content or event digest in the form algorithm:encoded-value. Version 1.0 requires sha-256.
   */
  export type Digest1 = string;

  /**
   * The shared envelope every artifact-version payload extends, per ACT-1.0.md section 7.1 and PROMPT.md's Artifact Types requirements. Concrete type schemas under schemas/artifact/types/ set artifact_type to a fixed const and add a type-specific `data` object.
   */
  export interface ArtifactEnvelope {
    artifact_id: UUID;
    version_id: Digest;
    artifact_type: string;
    schema_version: string;
    protocol_version: 'act/1.0';
    authoring_actor: ActorRef;
    created_at_claim: string;
    content: ContentDescriptor;
    /**
     * Typed, many-to-many edges from this version to prior nodes it depends on. Empty only for a Genesis version.
     */
    lineage: {
      relation:
        'revises' | 'input-of' | 'output-of' | 'merges' | 'responds-to' | 'external-import-of';
      target_version_id: Digest1;
    }[];
    applicable_policy:
      | {
          policy_id: UUID1;
          policy_version: Digest1;
        }
      | {
          not_applicable: true;
          reason: string;
        };
    confidence_assessments: ConfidenceAssessment[];
    uncertainties: Uncertainty[];
    evidence_refs: Digest1[];
    /**
     * @minItems 1
     */
    signatures: [
      {
        key_id: string;
        algorithm: 'ed25519';
        signature: string;
      },
      ...{
        key_id: string;
        algorithm: 'ed25519';
        signature: string;
      }[],
    ];
    sensitivity: 'public' | 'internal' | 'confidential' | 'restricted';
    retention_policy_id: UUID1 | null;
    /**
     * Type-specific payload, defined by the concrete artifact type schema.
     */
    data: {};
    extensions?: {};
  }
  /**
   * A reference to the actor and signing key responsible for an event or attestation.
   */
  export interface ActorRef {
    actor_id: UUID1;
    /**
     * algorithm:fingerprint, e.g. ed25519:9f86d0...
     */
    key_id: string;
    display_name?: string;
  }
  /**
   * Describes artifact content separately from immutable event metadata, per ACT-1.0.md section 15.
   */
  export interface ContentDescriptor {
    media_type: string;
    byte_length: number;
    digest: Digest1;
    storage: {
      kind: 'inline' | 'filesystem' | 'database' | 'external';
      /**
       * base64-encoded content, present only when kind is inline
       */
      inline_value?: string;
      /**
       * opaque storage-provider locator, present when kind is filesystem/database/external
       */
      location?: string;
    };
    encryption?: {
      algorithm: 'none' | 'aes-256-gcm';
      key_id?: string;
      /**
       * base64-encoded nonce, required when algorithm is not none
       */
      nonce?: string;
    };
    sensitivity: 'public' | 'internal' | 'confidential' | 'restricted';
    retention_policy_id?: UUID1;
    availability_state: 'available' | 'redacted' | 'erased' | 'unavailable';
  }
  /**
   * A single-dimension, attributed confidence assessment, per ACT-1.0.md section 9.
   */
  export interface ConfidenceAssessment {
    dimension:
      | 'semantic'
      | 'requirement'
      | 'architectural'
      | 'implementation'
      | 'verification'
      | 'runtime'
      | 'source';
    score: number | 'unassessed';
    assessor: ActorRef;
    method: string;
    method_version: string;
    evidence: Digest1[];
    calibration_context?: string;
    timestamp: string;
    rationale: string;
  }
  /**
   * An uncertainty record, per ACT-1.0.md section 10.
   */
  export interface Uncertainty {
    id: UUID1;
    description: string;
    category:
      'known-unknown' | 'assumption' | 'speculation' | 'residual-risk' | 'human-input-required';
    source: ActorRef;
    introducing_transformation?: Digest1;
    /**
     * @minItems 1
     */
    affected_artifacts: [Digest1, ...Digest1[]];
    impact: 'low' | 'medium' | 'high' | 'critical';
    likelihood: 'low' | 'medium' | 'high';
    owner: ActorRef;
    status: 'open' | 'discharged' | 'accepted';
    resolution_criteria: string;
    inherited_from: UUID1[];
    resolving_evidence_or_decision?: Digest1 | null;
  }
}

// source: schemas/artifact/types/challenge.schema.json
namespace NS_artifact_types_challenge {
  /**
   * Wraps schemas/challenge/challenge.schema.json as a lineage-tracked artifact.
   */
  export type ChallengeArtifact = ArtifactEnvelope & {
    artifact_type: 'Challenge';
    data: {
      challenge: Challenge;
    };
  };
  /**
   * Logical artifact identity, stable across revisions.
   */
  export type UUID = string;
  /**
   * Immutable version identity, the digest of this record's canonical bytes excluding signatures.
   */
  export type Digest = string;
  /**
   * A UUID, RFC 9562. ACT logical identities MUST use UUIDv7 (time-ordered) when newly generated; this pattern accepts any RFC 9562 UUID so that imported/legacy identifiers are not rejected on shape alone.
   */
  export type UUID1 = string;
  /**
   * A content or event digest in the form algorithm:encoded-value. Version 1.0 requires sha-256.
   */
  export type Digest1 = string;

  /**
   * The shared envelope every artifact-version payload extends, per ACT-1.0.md section 7.1 and PROMPT.md's Artifact Types requirements. Concrete type schemas under schemas/artifact/types/ set artifact_type to a fixed const and add a type-specific `data` object.
   */
  export interface ArtifactEnvelope {
    artifact_id: UUID;
    version_id: Digest;
    artifact_type: string;
    schema_version: string;
    protocol_version: 'act/1.0';
    authoring_actor: ActorRef;
    created_at_claim: string;
    content: ContentDescriptor;
    /**
     * Typed, many-to-many edges from this version to prior nodes it depends on. Empty only for a Genesis version.
     */
    lineage: {
      relation:
        'revises' | 'input-of' | 'output-of' | 'merges' | 'responds-to' | 'external-import-of';
      target_version_id: Digest1;
    }[];
    applicable_policy:
      | {
          policy_id: UUID1;
          policy_version: Digest1;
        }
      | {
          not_applicable: true;
          reason: string;
        };
    confidence_assessments: ConfidenceAssessment[];
    uncertainties: Uncertainty[];
    evidence_refs: Digest1[];
    /**
     * @minItems 1
     */
    signatures: [
      {
        key_id: string;
        algorithm: 'ed25519';
        signature: string;
      },
      ...{
        key_id: string;
        algorithm: 'ed25519';
        signature: string;
      }[],
    ];
    sensitivity: 'public' | 'internal' | 'confidential' | 'restricted';
    retention_policy_id: UUID1 | null;
    /**
     * Type-specific payload, defined by the concrete artifact type schema.
     */
    data: {};
    extensions?: {};
  }
  /**
   * A reference to the actor and signing key responsible for an event or attestation.
   */
  export interface ActorRef {
    actor_id: UUID1;
    /**
     * algorithm:fingerprint, e.g. ed25519:9f86d0...
     */
    key_id: string;
    display_name?: string;
  }
  /**
   * Describes artifact content separately from immutable event metadata, per ACT-1.0.md section 15.
   */
  export interface ContentDescriptor {
    media_type: string;
    byte_length: number;
    digest: Digest1;
    storage: {
      kind: 'inline' | 'filesystem' | 'database' | 'external';
      /**
       * base64-encoded content, present only when kind is inline
       */
      inline_value?: string;
      /**
       * opaque storage-provider locator, present when kind is filesystem/database/external
       */
      location?: string;
    };
    encryption?: {
      algorithm: 'none' | 'aes-256-gcm';
      key_id?: string;
      /**
       * base64-encoded nonce, required when algorithm is not none
       */
      nonce?: string;
    };
    sensitivity: 'public' | 'internal' | 'confidential' | 'restricted';
    retention_policy_id?: UUID1;
    availability_state: 'available' | 'redacted' | 'erased' | 'unavailable';
  }
  /**
   * A single-dimension, attributed confidence assessment, per ACT-1.0.md section 9.
   */
  export interface ConfidenceAssessment {
    dimension:
      | 'semantic'
      | 'requirement'
      | 'architectural'
      | 'implementation'
      | 'verification'
      | 'runtime'
      | 'source';
    score: number | 'unassessed';
    assessor: ActorRef;
    method: string;
    method_version: string;
    evidence: Digest1[];
    calibration_context?: string;
    timestamp: string;
    rationale: string;
  }
  /**
   * An uncertainty record, per ACT-1.0.md section 10.
   */
  export interface Uncertainty {
    id: UUID1;
    description: string;
    category:
      'known-unknown' | 'assumption' | 'speculation' | 'residual-risk' | 'human-input-required';
    source: ActorRef;
    introducing_transformation?: Digest1;
    /**
     * @minItems 1
     */
    affected_artifacts: [Digest1, ...Digest1[]];
    impact: 'low' | 'medium' | 'high' | 'critical';
    likelihood: 'low' | 'medium' | 'high';
    owner: ActorRef;
    status: 'open' | 'discharged' | 'accepted';
    resolution_criteria: string;
    inherited_from: UUID1[];
    resolving_evidence_or_decision?: Digest1 | null;
  }
  /**
   * A first-class signed dispute attestation. See ACT-1.0.md section 11.4.
   */
  export interface Challenge {
    challenge_id: UUID1;
    disputed_claim: Digest1;
    challenger: ActorRef;
    grounds: string;
    evidence: Digest1[];
    requested_remedy: string;
    status: 'open' | 'resolved_upheld' | 'resolved_rejected' | 'resolved_remedied';
    resolution?: {
      resolved_by: ActorRef;
      resolved_at: string;
      rationale: string;
      remediating_event_id?: Digest1;
    };
    issued_at: string;
    signature: {
      key_id: string;
      algorithm: 'ed25519';
      signature: string;
    };
  }
}

// source: schemas/artifact/types/constraint.schema.json
namespace NS_artifact_types_constraint {
  /**
   * A boundary condition that limits acceptable solutions.
   */
  export type Constraint = ArtifactEnvelope & {
    artifact_type: 'Constraint';
    data: {
      statement: string;
      constraint_type: 'technical' | 'legal' | 'business' | 'resource';
      hard: boolean;
    };
  };
  /**
   * Logical artifact identity, stable across revisions.
   */
  export type UUID = string;
  /**
   * Immutable version identity, the digest of this record's canonical bytes excluding signatures.
   */
  export type Digest = string;
  /**
   * A UUID, RFC 9562. ACT logical identities MUST use UUIDv7 (time-ordered) when newly generated; this pattern accepts any RFC 9562 UUID so that imported/legacy identifiers are not rejected on shape alone.
   */
  export type UUID1 = string;
  /**
   * A content or event digest in the form algorithm:encoded-value. Version 1.0 requires sha-256.
   */
  export type Digest1 = string;

  /**
   * The shared envelope every artifact-version payload extends, per ACT-1.0.md section 7.1 and PROMPT.md's Artifact Types requirements. Concrete type schemas under schemas/artifact/types/ set artifact_type to a fixed const and add a type-specific `data` object.
   */
  export interface ArtifactEnvelope {
    artifact_id: UUID;
    version_id: Digest;
    artifact_type: string;
    schema_version: string;
    protocol_version: 'act/1.0';
    authoring_actor: ActorRef;
    created_at_claim: string;
    content: ContentDescriptor;
    /**
     * Typed, many-to-many edges from this version to prior nodes it depends on. Empty only for a Genesis version.
     */
    lineage: {
      relation:
        'revises' | 'input-of' | 'output-of' | 'merges' | 'responds-to' | 'external-import-of';
      target_version_id: Digest1;
    }[];
    applicable_policy:
      | {
          policy_id: UUID1;
          policy_version: Digest1;
        }
      | {
          not_applicable: true;
          reason: string;
        };
    confidence_assessments: ConfidenceAssessment[];
    uncertainties: Uncertainty[];
    evidence_refs: Digest1[];
    /**
     * @minItems 1
     */
    signatures: [
      {
        key_id: string;
        algorithm: 'ed25519';
        signature: string;
      },
      ...{
        key_id: string;
        algorithm: 'ed25519';
        signature: string;
      }[],
    ];
    sensitivity: 'public' | 'internal' | 'confidential' | 'restricted';
    retention_policy_id: UUID1 | null;
    /**
     * Type-specific payload, defined by the concrete artifact type schema.
     */
    data: {};
    extensions?: {};
  }
  /**
   * A reference to the actor and signing key responsible for an event or attestation.
   */
  export interface ActorRef {
    actor_id: UUID1;
    /**
     * algorithm:fingerprint, e.g. ed25519:9f86d0...
     */
    key_id: string;
    display_name?: string;
  }
  /**
   * Describes artifact content separately from immutable event metadata, per ACT-1.0.md section 15.
   */
  export interface ContentDescriptor {
    media_type: string;
    byte_length: number;
    digest: Digest1;
    storage: {
      kind: 'inline' | 'filesystem' | 'database' | 'external';
      /**
       * base64-encoded content, present only when kind is inline
       */
      inline_value?: string;
      /**
       * opaque storage-provider locator, present when kind is filesystem/database/external
       */
      location?: string;
    };
    encryption?: {
      algorithm: 'none' | 'aes-256-gcm';
      key_id?: string;
      /**
       * base64-encoded nonce, required when algorithm is not none
       */
      nonce?: string;
    };
    sensitivity: 'public' | 'internal' | 'confidential' | 'restricted';
    retention_policy_id?: UUID1;
    availability_state: 'available' | 'redacted' | 'erased' | 'unavailable';
  }
  /**
   * A single-dimension, attributed confidence assessment, per ACT-1.0.md section 9.
   */
  export interface ConfidenceAssessment {
    dimension:
      | 'semantic'
      | 'requirement'
      | 'architectural'
      | 'implementation'
      | 'verification'
      | 'runtime'
      | 'source';
    score: number | 'unassessed';
    assessor: ActorRef;
    method: string;
    method_version: string;
    evidence: Digest1[];
    calibration_context?: string;
    timestamp: string;
    rationale: string;
  }
  /**
   * An uncertainty record, per ACT-1.0.md section 10.
   */
  export interface Uncertainty {
    id: UUID1;
    description: string;
    category:
      'known-unknown' | 'assumption' | 'speculation' | 'residual-risk' | 'human-input-required';
    source: ActorRef;
    introducing_transformation?: Digest1;
    /**
     * @minItems 1
     */
    affected_artifacts: [Digest1, ...Digest1[]];
    impact: 'low' | 'medium' | 'high' | 'critical';
    likelihood: 'low' | 'medium' | 'high';
    owner: ActorRef;
    status: 'open' | 'discharged' | 'accepted';
    resolution_criteria: string;
    inherited_from: UUID1[];
    resolving_evidence_or_decision?: Digest1 | null;
  }
}

// source: schemas/artifact/types/decision.schema.json
namespace NS_artifact_types_decision {
  /**
   * A recorded choice among considered options.
   */
  export type Decision = ArtifactEnvelope & {
    artifact_type: 'Decision';
    data: {
      statement: string;
      /**
       * @minItems 1
       */
      options_considered: [string, ...string[]];
      chosen_option: string;
      rationale: string;
    };
  };
  /**
   * Logical artifact identity, stable across revisions.
   */
  export type UUID = string;
  /**
   * Immutable version identity, the digest of this record's canonical bytes excluding signatures.
   */
  export type Digest = string;
  /**
   * A UUID, RFC 9562. ACT logical identities MUST use UUIDv7 (time-ordered) when newly generated; this pattern accepts any RFC 9562 UUID so that imported/legacy identifiers are not rejected on shape alone.
   */
  export type UUID1 = string;
  /**
   * A content or event digest in the form algorithm:encoded-value. Version 1.0 requires sha-256.
   */
  export type Digest1 = string;

  /**
   * The shared envelope every artifact-version payload extends, per ACT-1.0.md section 7.1 and PROMPT.md's Artifact Types requirements. Concrete type schemas under schemas/artifact/types/ set artifact_type to a fixed const and add a type-specific `data` object.
   */
  export interface ArtifactEnvelope {
    artifact_id: UUID;
    version_id: Digest;
    artifact_type: string;
    schema_version: string;
    protocol_version: 'act/1.0';
    authoring_actor: ActorRef;
    created_at_claim: string;
    content: ContentDescriptor;
    /**
     * Typed, many-to-many edges from this version to prior nodes it depends on. Empty only for a Genesis version.
     */
    lineage: {
      relation:
        'revises' | 'input-of' | 'output-of' | 'merges' | 'responds-to' | 'external-import-of';
      target_version_id: Digest1;
    }[];
    applicable_policy:
      | {
          policy_id: UUID1;
          policy_version: Digest1;
        }
      | {
          not_applicable: true;
          reason: string;
        };
    confidence_assessments: ConfidenceAssessment[];
    uncertainties: Uncertainty[];
    evidence_refs: Digest1[];
    /**
     * @minItems 1
     */
    signatures: [
      {
        key_id: string;
        algorithm: 'ed25519';
        signature: string;
      },
      ...{
        key_id: string;
        algorithm: 'ed25519';
        signature: string;
      }[],
    ];
    sensitivity: 'public' | 'internal' | 'confidential' | 'restricted';
    retention_policy_id: UUID1 | null;
    /**
     * Type-specific payload, defined by the concrete artifact type schema.
     */
    data: {};
    extensions?: {};
  }
  /**
   * A reference to the actor and signing key responsible for an event or attestation.
   */
  export interface ActorRef {
    actor_id: UUID1;
    /**
     * algorithm:fingerprint, e.g. ed25519:9f86d0...
     */
    key_id: string;
    display_name?: string;
  }
  /**
   * Describes artifact content separately from immutable event metadata, per ACT-1.0.md section 15.
   */
  export interface ContentDescriptor {
    media_type: string;
    byte_length: number;
    digest: Digest1;
    storage: {
      kind: 'inline' | 'filesystem' | 'database' | 'external';
      /**
       * base64-encoded content, present only when kind is inline
       */
      inline_value?: string;
      /**
       * opaque storage-provider locator, present when kind is filesystem/database/external
       */
      location?: string;
    };
    encryption?: {
      algorithm: 'none' | 'aes-256-gcm';
      key_id?: string;
      /**
       * base64-encoded nonce, required when algorithm is not none
       */
      nonce?: string;
    };
    sensitivity: 'public' | 'internal' | 'confidential' | 'restricted';
    retention_policy_id?: UUID1;
    availability_state: 'available' | 'redacted' | 'erased' | 'unavailable';
  }
  /**
   * A single-dimension, attributed confidence assessment, per ACT-1.0.md section 9.
   */
  export interface ConfidenceAssessment {
    dimension:
      | 'semantic'
      | 'requirement'
      | 'architectural'
      | 'implementation'
      | 'verification'
      | 'runtime'
      | 'source';
    score: number | 'unassessed';
    assessor: ActorRef;
    method: string;
    method_version: string;
    evidence: Digest1[];
    calibration_context?: string;
    timestamp: string;
    rationale: string;
  }
  /**
   * An uncertainty record, per ACT-1.0.md section 10.
   */
  export interface Uncertainty {
    id: UUID1;
    description: string;
    category:
      'known-unknown' | 'assumption' | 'speculation' | 'residual-risk' | 'human-input-required';
    source: ActorRef;
    introducing_transformation?: Digest1;
    /**
     * @minItems 1
     */
    affected_artifacts: [Digest1, ...Digest1[]];
    impact: 'low' | 'medium' | 'high' | 'critical';
    likelihood: 'low' | 'medium' | 'high';
    owner: ActorRef;
    status: 'open' | 'discharged' | 'accepted';
    resolution_criteria: string;
    inherited_from: UUID1[];
    resolving_evidence_or_decision?: Digest1 | null;
  }
}

// source: schemas/artifact/types/evidence.schema.json
namespace NS_artifact_types_evidence {
  /**
   * Immutable or content-addressed support for a claim (ACT-1.0.md section 11.1).
   */
  export type Evidence = ArtifactEnvelope & {
    artifact_type: 'Evidence';
    data: {
      origin: string;
      collection_method: string;
      /**
       * @minItems 1
       */
      custody: [ActorRef, ...ActorRef[]];
      limitations: string;
    };
  };
  /**
   * Logical artifact identity, stable across revisions.
   */
  export type UUID = string;
  /**
   * Immutable version identity, the digest of this record's canonical bytes excluding signatures.
   */
  export type Digest = string;
  /**
   * A UUID, RFC 9562. ACT logical identities MUST use UUIDv7 (time-ordered) when newly generated; this pattern accepts any RFC 9562 UUID so that imported/legacy identifiers are not rejected on shape alone.
   */
  export type UUID1 = string;
  /**
   * A content or event digest in the form algorithm:encoded-value. Version 1.0 requires sha-256.
   */
  export type Digest1 = string;

  /**
   * The shared envelope every artifact-version payload extends, per ACT-1.0.md section 7.1 and PROMPT.md's Artifact Types requirements. Concrete type schemas under schemas/artifact/types/ set artifact_type to a fixed const and add a type-specific `data` object.
   */
  export interface ArtifactEnvelope {
    artifact_id: UUID;
    version_id: Digest;
    artifact_type: string;
    schema_version: string;
    protocol_version: 'act/1.0';
    authoring_actor: ActorRef;
    created_at_claim: string;
    content: ContentDescriptor;
    /**
     * Typed, many-to-many edges from this version to prior nodes it depends on. Empty only for a Genesis version.
     */
    lineage: {
      relation:
        'revises' | 'input-of' | 'output-of' | 'merges' | 'responds-to' | 'external-import-of';
      target_version_id: Digest1;
    }[];
    applicable_policy:
      | {
          policy_id: UUID1;
          policy_version: Digest1;
        }
      | {
          not_applicable: true;
          reason: string;
        };
    confidence_assessments: ConfidenceAssessment[];
    uncertainties: Uncertainty[];
    evidence_refs: Digest1[];
    /**
     * @minItems 1
     */
    signatures: [
      {
        key_id: string;
        algorithm: 'ed25519';
        signature: string;
      },
      ...{
        key_id: string;
        algorithm: 'ed25519';
        signature: string;
      }[],
    ];
    sensitivity: 'public' | 'internal' | 'confidential' | 'restricted';
    retention_policy_id: UUID1 | null;
    /**
     * Type-specific payload, defined by the concrete artifact type schema.
     */
    data: {};
    extensions?: {};
  }
  /**
   * A reference to the actor and signing key responsible for an event or attestation.
   */
  export interface ActorRef {
    actor_id: UUID1;
    /**
     * algorithm:fingerprint, e.g. ed25519:9f86d0...
     */
    key_id: string;
    display_name?: string;
  }
  /**
   * Describes artifact content separately from immutable event metadata, per ACT-1.0.md section 15.
   */
  export interface ContentDescriptor {
    media_type: string;
    byte_length: number;
    digest: Digest1;
    storage: {
      kind: 'inline' | 'filesystem' | 'database' | 'external';
      /**
       * base64-encoded content, present only when kind is inline
       */
      inline_value?: string;
      /**
       * opaque storage-provider locator, present when kind is filesystem/database/external
       */
      location?: string;
    };
    encryption?: {
      algorithm: 'none' | 'aes-256-gcm';
      key_id?: string;
      /**
       * base64-encoded nonce, required when algorithm is not none
       */
      nonce?: string;
    };
    sensitivity: 'public' | 'internal' | 'confidential' | 'restricted';
    retention_policy_id?: UUID1;
    availability_state: 'available' | 'redacted' | 'erased' | 'unavailable';
  }
  /**
   * A single-dimension, attributed confidence assessment, per ACT-1.0.md section 9.
   */
  export interface ConfidenceAssessment {
    dimension:
      | 'semantic'
      | 'requirement'
      | 'architectural'
      | 'implementation'
      | 'verification'
      | 'runtime'
      | 'source';
    score: number | 'unassessed';
    assessor: ActorRef;
    method: string;
    method_version: string;
    evidence: Digest1[];
    calibration_context?: string;
    timestamp: string;
    rationale: string;
  }
  /**
   * An uncertainty record, per ACT-1.0.md section 10.
   */
  export interface Uncertainty {
    id: UUID1;
    description: string;
    category:
      'known-unknown' | 'assumption' | 'speculation' | 'residual-risk' | 'human-input-required';
    source: ActorRef;
    introducing_transformation?: Digest1;
    /**
     * @minItems 1
     */
    affected_artifacts: [Digest1, ...Digest1[]];
    impact: 'low' | 'medium' | 'high' | 'critical';
    likelihood: 'low' | 'medium' | 'high';
    owner: ActorRef;
    status: 'open' | 'discharged' | 'accepted';
    resolution_criteria: string;
    inherited_from: UUID1[];
    resolving_evidence_or_decision?: Digest1 | null;
  }
}

// source: schemas/artifact/types/goal.schema.json
namespace NS_artifact_types_goal {
  /**
   * A measurable objective in service of an Intent.
   */
  export type Goal = ArtifactEnvelope & {
    artifact_type: 'Goal';
    data: {
      statement: string;
      /**
       * @minItems 1
       */
      success_criteria: [string, ...string[]];
      parent_intent_id?: UUID1;
    };
  };
  /**
   * Logical artifact identity, stable across revisions.
   */
  export type UUID = string;
  /**
   * Immutable version identity, the digest of this record's canonical bytes excluding signatures.
   */
  export type Digest = string;
  /**
   * A UUID, RFC 9562. ACT logical identities MUST use UUIDv7 (time-ordered) when newly generated; this pattern accepts any RFC 9562 UUID so that imported/legacy identifiers are not rejected on shape alone.
   */
  export type UUID1 = string;
  /**
   * A content or event digest in the form algorithm:encoded-value. Version 1.0 requires sha-256.
   */
  export type Digest1 = string;

  /**
   * The shared envelope every artifact-version payload extends, per ACT-1.0.md section 7.1 and PROMPT.md's Artifact Types requirements. Concrete type schemas under schemas/artifact/types/ set artifact_type to a fixed const and add a type-specific `data` object.
   */
  export interface ArtifactEnvelope {
    artifact_id: UUID;
    version_id: Digest;
    artifact_type: string;
    schema_version: string;
    protocol_version: 'act/1.0';
    authoring_actor: ActorRef;
    created_at_claim: string;
    content: ContentDescriptor;
    /**
     * Typed, many-to-many edges from this version to prior nodes it depends on. Empty only for a Genesis version.
     */
    lineage: {
      relation:
        'revises' | 'input-of' | 'output-of' | 'merges' | 'responds-to' | 'external-import-of';
      target_version_id: Digest1;
    }[];
    applicable_policy:
      | {
          policy_id: UUID1;
          policy_version: Digest1;
        }
      | {
          not_applicable: true;
          reason: string;
        };
    confidence_assessments: ConfidenceAssessment[];
    uncertainties: Uncertainty[];
    evidence_refs: Digest1[];
    /**
     * @minItems 1
     */
    signatures: [
      {
        key_id: string;
        algorithm: 'ed25519';
        signature: string;
      },
      ...{
        key_id: string;
        algorithm: 'ed25519';
        signature: string;
      }[],
    ];
    sensitivity: 'public' | 'internal' | 'confidential' | 'restricted';
    retention_policy_id: UUID1 | null;
    /**
     * Type-specific payload, defined by the concrete artifact type schema.
     */
    data: {};
    extensions?: {};
  }
  /**
   * A reference to the actor and signing key responsible for an event or attestation.
   */
  export interface ActorRef {
    actor_id: UUID1;
    /**
     * algorithm:fingerprint, e.g. ed25519:9f86d0...
     */
    key_id: string;
    display_name?: string;
  }
  /**
   * Describes artifact content separately from immutable event metadata, per ACT-1.0.md section 15.
   */
  export interface ContentDescriptor {
    media_type: string;
    byte_length: number;
    digest: Digest1;
    storage: {
      kind: 'inline' | 'filesystem' | 'database' | 'external';
      /**
       * base64-encoded content, present only when kind is inline
       */
      inline_value?: string;
      /**
       * opaque storage-provider locator, present when kind is filesystem/database/external
       */
      location?: string;
    };
    encryption?: {
      algorithm: 'none' | 'aes-256-gcm';
      key_id?: string;
      /**
       * base64-encoded nonce, required when algorithm is not none
       */
      nonce?: string;
    };
    sensitivity: 'public' | 'internal' | 'confidential' | 'restricted';
    retention_policy_id?: UUID1;
    availability_state: 'available' | 'redacted' | 'erased' | 'unavailable';
  }
  /**
   * A single-dimension, attributed confidence assessment, per ACT-1.0.md section 9.
   */
  export interface ConfidenceAssessment {
    dimension:
      | 'semantic'
      | 'requirement'
      | 'architectural'
      | 'implementation'
      | 'verification'
      | 'runtime'
      | 'source';
    score: number | 'unassessed';
    assessor: ActorRef;
    method: string;
    method_version: string;
    evidence: Digest1[];
    calibration_context?: string;
    timestamp: string;
    rationale: string;
  }
  /**
   * An uncertainty record, per ACT-1.0.md section 10.
   */
  export interface Uncertainty {
    id: UUID1;
    description: string;
    category:
      'known-unknown' | 'assumption' | 'speculation' | 'residual-risk' | 'human-input-required';
    source: ActorRef;
    introducing_transformation?: Digest1;
    /**
     * @minItems 1
     */
    affected_artifacts: [Digest1, ...Digest1[]];
    impact: 'low' | 'medium' | 'high' | 'critical';
    likelihood: 'low' | 'medium' | 'high';
    owner: ActorRef;
    status: 'open' | 'discharged' | 'accepted';
    resolution_criteria: string;
    inherited_from: UUID1[];
    resolving_evidence_or_decision?: Digest1 | null;
  }
}

// source: schemas/artifact/types/human-proposal.schema.json
namespace NS_artifact_types_human_proposal {
  /**
   * A proposal authored by a human.
   */
  export type HumanProposal = ArtifactEnvelope & {
    artifact_type: 'HumanProposal';
    data: {
      proposer: ActorRef;
      content_digest: Digest1;
      rationale: string;
    };
  };
  /**
   * Logical artifact identity, stable across revisions.
   */
  export type UUID = string;
  /**
   * Immutable version identity, the digest of this record's canonical bytes excluding signatures.
   */
  export type Digest = string;
  /**
   * A UUID, RFC 9562. ACT logical identities MUST use UUIDv7 (time-ordered) when newly generated; this pattern accepts any RFC 9562 UUID so that imported/legacy identifiers are not rejected on shape alone.
   */
  export type UUID1 = string;
  /**
   * A content or event digest in the form algorithm:encoded-value. Version 1.0 requires sha-256.
   */
  export type Digest1 = string;

  /**
   * The shared envelope every artifact-version payload extends, per ACT-1.0.md section 7.1 and PROMPT.md's Artifact Types requirements. Concrete type schemas under schemas/artifact/types/ set artifact_type to a fixed const and add a type-specific `data` object.
   */
  export interface ArtifactEnvelope {
    artifact_id: UUID;
    version_id: Digest;
    artifact_type: string;
    schema_version: string;
    protocol_version: 'act/1.0';
    authoring_actor: ActorRef;
    created_at_claim: string;
    content: ContentDescriptor;
    /**
     * Typed, many-to-many edges from this version to prior nodes it depends on. Empty only for a Genesis version.
     */
    lineage: {
      relation:
        'revises' | 'input-of' | 'output-of' | 'merges' | 'responds-to' | 'external-import-of';
      target_version_id: Digest1;
    }[];
    applicable_policy:
      | {
          policy_id: UUID1;
          policy_version: Digest1;
        }
      | {
          not_applicable: true;
          reason: string;
        };
    confidence_assessments: ConfidenceAssessment[];
    uncertainties: Uncertainty[];
    evidence_refs: Digest1[];
    /**
     * @minItems 1
     */
    signatures: [
      {
        key_id: string;
        algorithm: 'ed25519';
        signature: string;
      },
      ...{
        key_id: string;
        algorithm: 'ed25519';
        signature: string;
      }[],
    ];
    sensitivity: 'public' | 'internal' | 'confidential' | 'restricted';
    retention_policy_id: UUID1 | null;
    /**
     * Type-specific payload, defined by the concrete artifact type schema.
     */
    data: {};
    extensions?: {};
  }
  /**
   * A reference to the actor and signing key responsible for an event or attestation.
   */
  export interface ActorRef {
    actor_id: UUID1;
    /**
     * algorithm:fingerprint, e.g. ed25519:9f86d0...
     */
    key_id: string;
    display_name?: string;
  }
  /**
   * Describes artifact content separately from immutable event metadata, per ACT-1.0.md section 15.
   */
  export interface ContentDescriptor {
    media_type: string;
    byte_length: number;
    digest: Digest1;
    storage: {
      kind: 'inline' | 'filesystem' | 'database' | 'external';
      /**
       * base64-encoded content, present only when kind is inline
       */
      inline_value?: string;
      /**
       * opaque storage-provider locator, present when kind is filesystem/database/external
       */
      location?: string;
    };
    encryption?: {
      algorithm: 'none' | 'aes-256-gcm';
      key_id?: string;
      /**
       * base64-encoded nonce, required when algorithm is not none
       */
      nonce?: string;
    };
    sensitivity: 'public' | 'internal' | 'confidential' | 'restricted';
    retention_policy_id?: UUID1;
    availability_state: 'available' | 'redacted' | 'erased' | 'unavailable';
  }
  /**
   * A single-dimension, attributed confidence assessment, per ACT-1.0.md section 9.
   */
  export interface ConfidenceAssessment {
    dimension:
      | 'semantic'
      | 'requirement'
      | 'architectural'
      | 'implementation'
      | 'verification'
      | 'runtime'
      | 'source';
    score: number | 'unassessed';
    assessor: ActorRef;
    method: string;
    method_version: string;
    evidence: Digest1[];
    calibration_context?: string;
    timestamp: string;
    rationale: string;
  }
  /**
   * An uncertainty record, per ACT-1.0.md section 10.
   */
  export interface Uncertainty {
    id: UUID1;
    description: string;
    category:
      'known-unknown' | 'assumption' | 'speculation' | 'residual-risk' | 'human-input-required';
    source: ActorRef;
    introducing_transformation?: Digest1;
    /**
     * @minItems 1
     */
    affected_artifacts: [Digest1, ...Digest1[]];
    impact: 'low' | 'medium' | 'high' | 'critical';
    likelihood: 'low' | 'medium' | 'high';
    owner: ActorRef;
    status: 'open' | 'discharged' | 'accepted';
    resolution_criteria: string;
    inherited_from: UUID1[];
    resolving_evidence_or_decision?: Digest1 | null;
  }
}

// source: schemas/artifact/types/intent.schema.json
namespace NS_artifact_types_intent {
  /**
   * A stated purpose or objective, the root or a revision in an intent lineage (ACT-1.0.md section 7.3).
   */
  export type Intent = ArtifactEnvelope & {
    artifact_type: 'Intent';
    data: {
      statement: string;
      scope: string;
      priority?: 'low' | 'medium' | 'high' | 'critical';
    };
  };
  /**
   * Logical artifact identity, stable across revisions.
   */
  export type UUID = string;
  /**
   * Immutable version identity, the digest of this record's canonical bytes excluding signatures.
   */
  export type Digest = string;
  /**
   * A UUID, RFC 9562. ACT logical identities MUST use UUIDv7 (time-ordered) when newly generated; this pattern accepts any RFC 9562 UUID so that imported/legacy identifiers are not rejected on shape alone.
   */
  export type UUID1 = string;
  /**
   * A content or event digest in the form algorithm:encoded-value. Version 1.0 requires sha-256.
   */
  export type Digest1 = string;

  /**
   * The shared envelope every artifact-version payload extends, per ACT-1.0.md section 7.1 and PROMPT.md's Artifact Types requirements. Concrete type schemas under schemas/artifact/types/ set artifact_type to a fixed const and add a type-specific `data` object.
   */
  export interface ArtifactEnvelope {
    artifact_id: UUID;
    version_id: Digest;
    artifact_type: string;
    schema_version: string;
    protocol_version: 'act/1.0';
    authoring_actor: ActorRef;
    created_at_claim: string;
    content: ContentDescriptor;
    /**
     * Typed, many-to-many edges from this version to prior nodes it depends on. Empty only for a Genesis version.
     */
    lineage: {
      relation:
        'revises' | 'input-of' | 'output-of' | 'merges' | 'responds-to' | 'external-import-of';
      target_version_id: Digest1;
    }[];
    applicable_policy:
      | {
          policy_id: UUID1;
          policy_version: Digest1;
        }
      | {
          not_applicable: true;
          reason: string;
        };
    confidence_assessments: ConfidenceAssessment[];
    uncertainties: Uncertainty[];
    evidence_refs: Digest1[];
    /**
     * @minItems 1
     */
    signatures: [
      {
        key_id: string;
        algorithm: 'ed25519';
        signature: string;
      },
      ...{
        key_id: string;
        algorithm: 'ed25519';
        signature: string;
      }[],
    ];
    sensitivity: 'public' | 'internal' | 'confidential' | 'restricted';
    retention_policy_id: UUID1 | null;
    /**
     * Type-specific payload, defined by the concrete artifact type schema.
     */
    data: {};
    extensions?: {};
  }
  /**
   * A reference to the actor and signing key responsible for an event or attestation.
   */
  export interface ActorRef {
    actor_id: UUID1;
    /**
     * algorithm:fingerprint, e.g. ed25519:9f86d0...
     */
    key_id: string;
    display_name?: string;
  }
  /**
   * Describes artifact content separately from immutable event metadata, per ACT-1.0.md section 15.
   */
  export interface ContentDescriptor {
    media_type: string;
    byte_length: number;
    digest: Digest1;
    storage: {
      kind: 'inline' | 'filesystem' | 'database' | 'external';
      /**
       * base64-encoded content, present only when kind is inline
       */
      inline_value?: string;
      /**
       * opaque storage-provider locator, present when kind is filesystem/database/external
       */
      location?: string;
    };
    encryption?: {
      algorithm: 'none' | 'aes-256-gcm';
      key_id?: string;
      /**
       * base64-encoded nonce, required when algorithm is not none
       */
      nonce?: string;
    };
    sensitivity: 'public' | 'internal' | 'confidential' | 'restricted';
    retention_policy_id?: UUID1;
    availability_state: 'available' | 'redacted' | 'erased' | 'unavailable';
  }
  /**
   * A single-dimension, attributed confidence assessment, per ACT-1.0.md section 9.
   */
  export interface ConfidenceAssessment {
    dimension:
      | 'semantic'
      | 'requirement'
      | 'architectural'
      | 'implementation'
      | 'verification'
      | 'runtime'
      | 'source';
    score: number | 'unassessed';
    assessor: ActorRef;
    method: string;
    method_version: string;
    evidence: Digest1[];
    calibration_context?: string;
    timestamp: string;
    rationale: string;
  }
  /**
   * An uncertainty record, per ACT-1.0.md section 10.
   */
  export interface Uncertainty {
    id: UUID1;
    description: string;
    category:
      'known-unknown' | 'assumption' | 'speculation' | 'residual-risk' | 'human-input-required';
    source: ActorRef;
    introducing_transformation?: Digest1;
    /**
     * @minItems 1
     */
    affected_artifacts: [Digest1, ...Digest1[]];
    impact: 'low' | 'medium' | 'high' | 'critical';
    likelihood: 'low' | 'medium' | 'high';
    owner: ActorRef;
    status: 'open' | 'discharged' | 'accepted';
    resolution_criteria: string;
    inherited_from: UUID1[];
    resolving_evidence_or_decision?: Digest1 | null;
  }
}

// source: schemas/artifact/types/key.schema.json
namespace NS_artifact_types_key {
  /**
   * A cryptographic key bound to an actor.
   */
  export type Key = ArtifactEnvelope & {
    artifact_type: 'Key';
    data: {
      key_id: string;
      algorithm: 'ed25519';
      public_key: string;
      status: 'issued' | 'active' | 'rotated' | 'expired' | 'revoked' | 'compromised';
      owner_actor_id: UUID1;
    };
  };
  /**
   * Logical artifact identity, stable across revisions.
   */
  export type UUID = string;
  /**
   * Immutable version identity, the digest of this record's canonical bytes excluding signatures.
   */
  export type Digest = string;
  /**
   * A UUID, RFC 9562. ACT logical identities MUST use UUIDv7 (time-ordered) when newly generated; this pattern accepts any RFC 9562 UUID so that imported/legacy identifiers are not rejected on shape alone.
   */
  export type UUID1 = string;
  /**
   * A content or event digest in the form algorithm:encoded-value. Version 1.0 requires sha-256.
   */
  export type Digest1 = string;

  /**
   * The shared envelope every artifact-version payload extends, per ACT-1.0.md section 7.1 and PROMPT.md's Artifact Types requirements. Concrete type schemas under schemas/artifact/types/ set artifact_type to a fixed const and add a type-specific `data` object.
   */
  export interface ArtifactEnvelope {
    artifact_id: UUID;
    version_id: Digest;
    artifact_type: string;
    schema_version: string;
    protocol_version: 'act/1.0';
    authoring_actor: ActorRef;
    created_at_claim: string;
    content: ContentDescriptor;
    /**
     * Typed, many-to-many edges from this version to prior nodes it depends on. Empty only for a Genesis version.
     */
    lineage: {
      relation:
        'revises' | 'input-of' | 'output-of' | 'merges' | 'responds-to' | 'external-import-of';
      target_version_id: Digest1;
    }[];
    applicable_policy:
      | {
          policy_id: UUID1;
          policy_version: Digest1;
        }
      | {
          not_applicable: true;
          reason: string;
        };
    confidence_assessments: ConfidenceAssessment[];
    uncertainties: Uncertainty[];
    evidence_refs: Digest1[];
    /**
     * @minItems 1
     */
    signatures: [
      {
        key_id: string;
        algorithm: 'ed25519';
        signature: string;
      },
      ...{
        key_id: string;
        algorithm: 'ed25519';
        signature: string;
      }[],
    ];
    sensitivity: 'public' | 'internal' | 'confidential' | 'restricted';
    retention_policy_id: UUID1 | null;
    /**
     * Type-specific payload, defined by the concrete artifact type schema.
     */
    data: {};
    extensions?: {};
  }
  /**
   * A reference to the actor and signing key responsible for an event or attestation.
   */
  export interface ActorRef {
    actor_id: UUID1;
    /**
     * algorithm:fingerprint, e.g. ed25519:9f86d0...
     */
    key_id: string;
    display_name?: string;
  }
  /**
   * Describes artifact content separately from immutable event metadata, per ACT-1.0.md section 15.
   */
  export interface ContentDescriptor {
    media_type: string;
    byte_length: number;
    digest: Digest1;
    storage: {
      kind: 'inline' | 'filesystem' | 'database' | 'external';
      /**
       * base64-encoded content, present only when kind is inline
       */
      inline_value?: string;
      /**
       * opaque storage-provider locator, present when kind is filesystem/database/external
       */
      location?: string;
    };
    encryption?: {
      algorithm: 'none' | 'aes-256-gcm';
      key_id?: string;
      /**
       * base64-encoded nonce, required when algorithm is not none
       */
      nonce?: string;
    };
    sensitivity: 'public' | 'internal' | 'confidential' | 'restricted';
    retention_policy_id?: UUID1;
    availability_state: 'available' | 'redacted' | 'erased' | 'unavailable';
  }
  /**
   * A single-dimension, attributed confidence assessment, per ACT-1.0.md section 9.
   */
  export interface ConfidenceAssessment {
    dimension:
      | 'semantic'
      | 'requirement'
      | 'architectural'
      | 'implementation'
      | 'verification'
      | 'runtime'
      | 'source';
    score: number | 'unassessed';
    assessor: ActorRef;
    method: string;
    method_version: string;
    evidence: Digest1[];
    calibration_context?: string;
    timestamp: string;
    rationale: string;
  }
  /**
   * An uncertainty record, per ACT-1.0.md section 10.
   */
  export interface Uncertainty {
    id: UUID1;
    description: string;
    category:
      'known-unknown' | 'assumption' | 'speculation' | 'residual-risk' | 'human-input-required';
    source: ActorRef;
    introducing_transformation?: Digest1;
    /**
     * @minItems 1
     */
    affected_artifacts: [Digest1, ...Digest1[]];
    impact: 'low' | 'medium' | 'high' | 'critical';
    likelihood: 'low' | 'medium' | 'high';
    owner: ActorRef;
    status: 'open' | 'discharged' | 'accepted';
    resolution_criteria: string;
    inherited_from: UUID1[];
    resolving_evidence_or_decision?: Digest1 | null;
  }
}

// source: schemas/artifact/types/policy.schema.json
namespace NS_artifact_types_policy {
  /**
   * Wraps schemas/policy/policy.schema.json as a lineage-tracked artifact.
   */
  export type PolicyArtifact = ArtifactEnvelope & {
    artifact_type: 'Policy';
    data: {
      policy: Policy;
    };
  };
  /**
   * Logical artifact identity, stable across revisions.
   */
  export type UUID = string;
  /**
   * Immutable version identity, the digest of this record's canonical bytes excluding signatures.
   */
  export type Digest = string;
  /**
   * A UUID, RFC 9562. ACT logical identities MUST use UUIDv7 (time-ordered) when newly generated; this pattern accepts any RFC 9562 UUID so that imported/legacy identifiers are not rejected on shape alone.
   */
  export type UUID1 = string;
  /**
   * A content or event digest in the form algorithm:encoded-value. Version 1.0 requires sha-256.
   */
  export type Digest1 = string;

  /**
   * The shared envelope every artifact-version payload extends, per ACT-1.0.md section 7.1 and PROMPT.md's Artifact Types requirements. Concrete type schemas under schemas/artifact/types/ set artifact_type to a fixed const and add a type-specific `data` object.
   */
  export interface ArtifactEnvelope {
    artifact_id: UUID;
    version_id: Digest;
    artifact_type: string;
    schema_version: string;
    protocol_version: 'act/1.0';
    authoring_actor: ActorRef;
    created_at_claim: string;
    content: ContentDescriptor;
    /**
     * Typed, many-to-many edges from this version to prior nodes it depends on. Empty only for a Genesis version.
     */
    lineage: {
      relation:
        'revises' | 'input-of' | 'output-of' | 'merges' | 'responds-to' | 'external-import-of';
      target_version_id: Digest1;
    }[];
    applicable_policy:
      | {
          policy_id: UUID1;
          policy_version: Digest1;
        }
      | {
          not_applicable: true;
          reason: string;
        };
    confidence_assessments: ConfidenceAssessment[];
    uncertainties: Uncertainty[];
    evidence_refs: Digest1[];
    /**
     * @minItems 1
     */
    signatures: [
      {
        key_id: string;
        algorithm: 'ed25519';
        signature: string;
      },
      ...{
        key_id: string;
        algorithm: 'ed25519';
        signature: string;
      }[],
    ];
    sensitivity: 'public' | 'internal' | 'confidential' | 'restricted';
    retention_policy_id: UUID1 | null;
    /**
     * Type-specific payload, defined by the concrete artifact type schema.
     */
    data: {};
    extensions?: {};
  }
  /**
   * A reference to the actor and signing key responsible for an event or attestation.
   */
  export interface ActorRef {
    actor_id: UUID1;
    /**
     * algorithm:fingerprint, e.g. ed25519:9f86d0...
     */
    key_id: string;
    display_name?: string;
  }
  /**
   * Describes artifact content separately from immutable event metadata, per ACT-1.0.md section 15.
   */
  export interface ContentDescriptor {
    media_type: string;
    byte_length: number;
    digest: Digest1;
    storage: {
      kind: 'inline' | 'filesystem' | 'database' | 'external';
      /**
       * base64-encoded content, present only when kind is inline
       */
      inline_value?: string;
      /**
       * opaque storage-provider locator, present when kind is filesystem/database/external
       */
      location?: string;
    };
    encryption?: {
      algorithm: 'none' | 'aes-256-gcm';
      key_id?: string;
      /**
       * base64-encoded nonce, required when algorithm is not none
       */
      nonce?: string;
    };
    sensitivity: 'public' | 'internal' | 'confidential' | 'restricted';
    retention_policy_id?: UUID1;
    availability_state: 'available' | 'redacted' | 'erased' | 'unavailable';
  }
  /**
   * A single-dimension, attributed confidence assessment, per ACT-1.0.md section 9.
   */
  export interface ConfidenceAssessment {
    dimension:
      | 'semantic'
      | 'requirement'
      | 'architectural'
      | 'implementation'
      | 'verification'
      | 'runtime'
      | 'source';
    score: number | 'unassessed';
    assessor: ActorRef;
    method: string;
    method_version: string;
    evidence: Digest1[];
    calibration_context?: string;
    timestamp: string;
    rationale: string;
  }
  /**
   * An uncertainty record, per ACT-1.0.md section 10.
   */
  export interface Uncertainty {
    id: UUID1;
    description: string;
    category:
      'known-unknown' | 'assumption' | 'speculation' | 'residual-risk' | 'human-input-required';
    source: ActorRef;
    introducing_transformation?: Digest1;
    /**
     * @minItems 1
     */
    affected_artifacts: [Digest1, ...Digest1[]];
    impact: 'low' | 'medium' | 'high' | 'critical';
    likelihood: 'low' | 'medium' | 'high';
    owner: ActorRef;
    status: 'open' | 'discharged' | 'accepted';
    resolution_criteria: string;
    inherited_from: UUID1[];
    resolving_evidence_or_decision?: Digest1 | null;
  }
  /**
   * A versioned, immutable policy document governing authorization, approval, retention, verification, or trust. See ACT-1.0.md section 12.
   */
  export interface Policy {
    policy_id: UUID1;
    policy_version: Digest1;
    kind: 'authorization' | 'approval' | 'retention' | 'verification' | 'trust' | 'authority';
    name: string;
    description?: string;
    issued_by: ActorRef;
    issued_at: string;
    supersedes?: Digest1 | null;
    /**
     * @minItems 1
     */
    rules: [
      {
        rule_id: string;
        /**
         * A declarative match predicate over subject type, semantic_change_claim.classification, artifact_type, sensitivity, or tenant.
         */
        when: {
          subject_kind?: string[];
          artifact_type?: string[];
          semantic_change_classification?: string[];
          sensitivity?: string[];
        };
        require: {
          approval?: boolean;
          quorum?: number;
          reviewer_roles?: string[];
          /**
           * If true, the reviewer MUST NOT be the same actor as the transformation's author.
           */
          separation_of_duties?: boolean;
        };
      },
      ...{
        rule_id: string;
        /**
         * A declarative match predicate over subject type, semantic_change_claim.classification, artifact_type, sensitivity, or tenant.
         */
        when: {
          subject_kind?: string[];
          artifact_type?: string[];
          semantic_change_classification?: string[];
          sensitivity?: string[];
        };
        require: {
          approval?: boolean;
          quorum?: number;
          reviewer_roles?: string[];
          /**
           * If true, the reviewer MUST NOT be the same actor as the transformation's author.
           */
          separation_of_duties?: boolean;
        };
      }[],
    ];
    /**
     * @minItems 1
     */
    signatures: [
      {
        key_id: string;
        algorithm: 'ed25519';
        signature: string;
      },
      ...{
        key_id: string;
        algorithm: 'ed25519';
        signature: string;
      }[],
    ];
  }
}

// source: schemas/artifact/types/prompt.schema.json
namespace NS_artifact_types_prompt {
  /**
   * A recorded prompt exchanged with an AI system, redactable per ACT-1.0.md section 15.
   */
  export type Prompt = ArtifactEnvelope & {
    artifact_type: 'Prompt';
    data: {
      role: 'system' | 'user' | 'assistant' | 'tool';
      content: string;
      redacted: boolean;
    };
  };
  /**
   * Logical artifact identity, stable across revisions.
   */
  export type UUID = string;
  /**
   * Immutable version identity, the digest of this record's canonical bytes excluding signatures.
   */
  export type Digest = string;
  /**
   * A UUID, RFC 9562. ACT logical identities MUST use UUIDv7 (time-ordered) when newly generated; this pattern accepts any RFC 9562 UUID so that imported/legacy identifiers are not rejected on shape alone.
   */
  export type UUID1 = string;
  /**
   * A content or event digest in the form algorithm:encoded-value. Version 1.0 requires sha-256.
   */
  export type Digest1 = string;

  /**
   * The shared envelope every artifact-version payload extends, per ACT-1.0.md section 7.1 and PROMPT.md's Artifact Types requirements. Concrete type schemas under schemas/artifact/types/ set artifact_type to a fixed const and add a type-specific `data` object.
   */
  export interface ArtifactEnvelope {
    artifact_id: UUID;
    version_id: Digest;
    artifact_type: string;
    schema_version: string;
    protocol_version: 'act/1.0';
    authoring_actor: ActorRef;
    created_at_claim: string;
    content: ContentDescriptor;
    /**
     * Typed, many-to-many edges from this version to prior nodes it depends on. Empty only for a Genesis version.
     */
    lineage: {
      relation:
        'revises' | 'input-of' | 'output-of' | 'merges' | 'responds-to' | 'external-import-of';
      target_version_id: Digest1;
    }[];
    applicable_policy:
      | {
          policy_id: UUID1;
          policy_version: Digest1;
        }
      | {
          not_applicable: true;
          reason: string;
        };
    confidence_assessments: ConfidenceAssessment[];
    uncertainties: Uncertainty[];
    evidence_refs: Digest1[];
    /**
     * @minItems 1
     */
    signatures: [
      {
        key_id: string;
        algorithm: 'ed25519';
        signature: string;
      },
      ...{
        key_id: string;
        algorithm: 'ed25519';
        signature: string;
      }[],
    ];
    sensitivity: 'public' | 'internal' | 'confidential' | 'restricted';
    retention_policy_id: UUID1 | null;
    /**
     * Type-specific payload, defined by the concrete artifact type schema.
     */
    data: {};
    extensions?: {};
  }
  /**
   * A reference to the actor and signing key responsible for an event or attestation.
   */
  export interface ActorRef {
    actor_id: UUID1;
    /**
     * algorithm:fingerprint, e.g. ed25519:9f86d0...
     */
    key_id: string;
    display_name?: string;
  }
  /**
   * Describes artifact content separately from immutable event metadata, per ACT-1.0.md section 15.
   */
  export interface ContentDescriptor {
    media_type: string;
    byte_length: number;
    digest: Digest1;
    storage: {
      kind: 'inline' | 'filesystem' | 'database' | 'external';
      /**
       * base64-encoded content, present only when kind is inline
       */
      inline_value?: string;
      /**
       * opaque storage-provider locator, present when kind is filesystem/database/external
       */
      location?: string;
    };
    encryption?: {
      algorithm: 'none' | 'aes-256-gcm';
      key_id?: string;
      /**
       * base64-encoded nonce, required when algorithm is not none
       */
      nonce?: string;
    };
    sensitivity: 'public' | 'internal' | 'confidential' | 'restricted';
    retention_policy_id?: UUID1;
    availability_state: 'available' | 'redacted' | 'erased' | 'unavailable';
  }
  /**
   * A single-dimension, attributed confidence assessment, per ACT-1.0.md section 9.
   */
  export interface ConfidenceAssessment {
    dimension:
      | 'semantic'
      | 'requirement'
      | 'architectural'
      | 'implementation'
      | 'verification'
      | 'runtime'
      | 'source';
    score: number | 'unassessed';
    assessor: ActorRef;
    method: string;
    method_version: string;
    evidence: Digest1[];
    calibration_context?: string;
    timestamp: string;
    rationale: string;
  }
  /**
   * An uncertainty record, per ACT-1.0.md section 10.
   */
  export interface Uncertainty {
    id: UUID1;
    description: string;
    category:
      'known-unknown' | 'assumption' | 'speculation' | 'residual-risk' | 'human-input-required';
    source: ActorRef;
    introducing_transformation?: Digest1;
    /**
     * @minItems 1
     */
    affected_artifacts: [Digest1, ...Digest1[]];
    impact: 'low' | 'medium' | 'high' | 'critical';
    likelihood: 'low' | 'medium' | 'high';
    owner: ActorRef;
    status: 'open' | 'discharged' | 'accepted';
    resolution_criteria: string;
    inherited_from: UUID1[];
    resolving_evidence_or_decision?: Digest1 | null;
  }
}

// source: schemas/artifact/types/requirement.schema.json
namespace NS_artifact_types_requirement {
  /**
   * A functional or non-functional requirement traceable to intent.
   */
  export type Requirement = ArtifactEnvelope & {
    artifact_type: 'Requirement';
    data: {
      statement: string;
      requirement_type: 'functional' | 'non-functional';
      priority: 'must' | 'should' | 'could' | 'wont';
      traces_to: UUID1[];
    };
  };
  /**
   * Logical artifact identity, stable across revisions.
   */
  export type UUID = string;
  /**
   * Immutable version identity, the digest of this record's canonical bytes excluding signatures.
   */
  export type Digest = string;
  /**
   * A UUID, RFC 9562. ACT logical identities MUST use UUIDv7 (time-ordered) when newly generated; this pattern accepts any RFC 9562 UUID so that imported/legacy identifiers are not rejected on shape alone.
   */
  export type UUID1 = string;
  /**
   * A content or event digest in the form algorithm:encoded-value. Version 1.0 requires sha-256.
   */
  export type Digest1 = string;

  /**
   * The shared envelope every artifact-version payload extends, per ACT-1.0.md section 7.1 and PROMPT.md's Artifact Types requirements. Concrete type schemas under schemas/artifact/types/ set artifact_type to a fixed const and add a type-specific `data` object.
   */
  export interface ArtifactEnvelope {
    artifact_id: UUID;
    version_id: Digest;
    artifact_type: string;
    schema_version: string;
    protocol_version: 'act/1.0';
    authoring_actor: ActorRef;
    created_at_claim: string;
    content: ContentDescriptor;
    /**
     * Typed, many-to-many edges from this version to prior nodes it depends on. Empty only for a Genesis version.
     */
    lineage: {
      relation:
        'revises' | 'input-of' | 'output-of' | 'merges' | 'responds-to' | 'external-import-of';
      target_version_id: Digest1;
    }[];
    applicable_policy:
      | {
          policy_id: UUID1;
          policy_version: Digest1;
        }
      | {
          not_applicable: true;
          reason: string;
        };
    confidence_assessments: ConfidenceAssessment[];
    uncertainties: Uncertainty[];
    evidence_refs: Digest1[];
    /**
     * @minItems 1
     */
    signatures: [
      {
        key_id: string;
        algorithm: 'ed25519';
        signature: string;
      },
      ...{
        key_id: string;
        algorithm: 'ed25519';
        signature: string;
      }[],
    ];
    sensitivity: 'public' | 'internal' | 'confidential' | 'restricted';
    retention_policy_id: UUID1 | null;
    /**
     * Type-specific payload, defined by the concrete artifact type schema.
     */
    data: {};
    extensions?: {};
  }
  /**
   * A reference to the actor and signing key responsible for an event or attestation.
   */
  export interface ActorRef {
    actor_id: UUID1;
    /**
     * algorithm:fingerprint, e.g. ed25519:9f86d0...
     */
    key_id: string;
    display_name?: string;
  }
  /**
   * Describes artifact content separately from immutable event metadata, per ACT-1.0.md section 15.
   */
  export interface ContentDescriptor {
    media_type: string;
    byte_length: number;
    digest: Digest1;
    storage: {
      kind: 'inline' | 'filesystem' | 'database' | 'external';
      /**
       * base64-encoded content, present only when kind is inline
       */
      inline_value?: string;
      /**
       * opaque storage-provider locator, present when kind is filesystem/database/external
       */
      location?: string;
    };
    encryption?: {
      algorithm: 'none' | 'aes-256-gcm';
      key_id?: string;
      /**
       * base64-encoded nonce, required when algorithm is not none
       */
      nonce?: string;
    };
    sensitivity: 'public' | 'internal' | 'confidential' | 'restricted';
    retention_policy_id?: UUID1;
    availability_state: 'available' | 'redacted' | 'erased' | 'unavailable';
  }
  /**
   * A single-dimension, attributed confidence assessment, per ACT-1.0.md section 9.
   */
  export interface ConfidenceAssessment {
    dimension:
      | 'semantic'
      | 'requirement'
      | 'architectural'
      | 'implementation'
      | 'verification'
      | 'runtime'
      | 'source';
    score: number | 'unassessed';
    assessor: ActorRef;
    method: string;
    method_version: string;
    evidence: Digest1[];
    calibration_context?: string;
    timestamp: string;
    rationale: string;
  }
  /**
   * An uncertainty record, per ACT-1.0.md section 10.
   */
  export interface Uncertainty {
    id: UUID1;
    description: string;
    category:
      'known-unknown' | 'assumption' | 'speculation' | 'residual-risk' | 'human-input-required';
    source: ActorRef;
    introducing_transformation?: Digest1;
    /**
     * @minItems 1
     */
    affected_artifacts: [Digest1, ...Digest1[]];
    impact: 'low' | 'medium' | 'high' | 'critical';
    likelihood: 'low' | 'medium' | 'high';
    owner: ActorRef;
    status: 'open' | 'discharged' | 'accepted';
    resolution_criteria: string;
    inherited_from: UUID1[];
    resolving_evidence_or_decision?: Digest1 | null;
  }
}

// source: schemas/artifact/types/revision.schema.json
namespace NS_artifact_types_revision {
  /**
   * Marks an artifact version as a revision of a prior version, independent of the generic lineage edge.
   */
  export type Revision = ArtifactEnvelope & {
    artifact_type: 'Revision';
    data: {
      revises_version_id: Digest1;
      revision_reason: string;
      revision_kind: 'clarification' | 'correction' | 'supersession' | 'merge';
    };
  };
  /**
   * Logical artifact identity, stable across revisions.
   */
  export type UUID = string;
  /**
   * Immutable version identity, the digest of this record's canonical bytes excluding signatures.
   */
  export type Digest = string;
  /**
   * A UUID, RFC 9562. ACT logical identities MUST use UUIDv7 (time-ordered) when newly generated; this pattern accepts any RFC 9562 UUID so that imported/legacy identifiers are not rejected on shape alone.
   */
  export type UUID1 = string;
  /**
   * A content or event digest in the form algorithm:encoded-value. Version 1.0 requires sha-256.
   */
  export type Digest1 = string;

  /**
   * The shared envelope every artifact-version payload extends, per ACT-1.0.md section 7.1 and PROMPT.md's Artifact Types requirements. Concrete type schemas under schemas/artifact/types/ set artifact_type to a fixed const and add a type-specific `data` object.
   */
  export interface ArtifactEnvelope {
    artifact_id: UUID;
    version_id: Digest;
    artifact_type: string;
    schema_version: string;
    protocol_version: 'act/1.0';
    authoring_actor: ActorRef;
    created_at_claim: string;
    content: ContentDescriptor;
    /**
     * Typed, many-to-many edges from this version to prior nodes it depends on. Empty only for a Genesis version.
     */
    lineage: {
      relation:
        'revises' | 'input-of' | 'output-of' | 'merges' | 'responds-to' | 'external-import-of';
      target_version_id: Digest1;
    }[];
    applicable_policy:
      | {
          policy_id: UUID1;
          policy_version: Digest1;
        }
      | {
          not_applicable: true;
          reason: string;
        };
    confidence_assessments: ConfidenceAssessment[];
    uncertainties: Uncertainty[];
    evidence_refs: Digest1[];
    /**
     * @minItems 1
     */
    signatures: [
      {
        key_id: string;
        algorithm: 'ed25519';
        signature: string;
      },
      ...{
        key_id: string;
        algorithm: 'ed25519';
        signature: string;
      }[],
    ];
    sensitivity: 'public' | 'internal' | 'confidential' | 'restricted';
    retention_policy_id: UUID1 | null;
    /**
     * Type-specific payload, defined by the concrete artifact type schema.
     */
    data: {};
    extensions?: {};
  }
  /**
   * A reference to the actor and signing key responsible for an event or attestation.
   */
  export interface ActorRef {
    actor_id: UUID1;
    /**
     * algorithm:fingerprint, e.g. ed25519:9f86d0...
     */
    key_id: string;
    display_name?: string;
  }
  /**
   * Describes artifact content separately from immutable event metadata, per ACT-1.0.md section 15.
   */
  export interface ContentDescriptor {
    media_type: string;
    byte_length: number;
    digest: Digest1;
    storage: {
      kind: 'inline' | 'filesystem' | 'database' | 'external';
      /**
       * base64-encoded content, present only when kind is inline
       */
      inline_value?: string;
      /**
       * opaque storage-provider locator, present when kind is filesystem/database/external
       */
      location?: string;
    };
    encryption?: {
      algorithm: 'none' | 'aes-256-gcm';
      key_id?: string;
      /**
       * base64-encoded nonce, required when algorithm is not none
       */
      nonce?: string;
    };
    sensitivity: 'public' | 'internal' | 'confidential' | 'restricted';
    retention_policy_id?: UUID1;
    availability_state: 'available' | 'redacted' | 'erased' | 'unavailable';
  }
  /**
   * A single-dimension, attributed confidence assessment, per ACT-1.0.md section 9.
   */
  export interface ConfidenceAssessment {
    dimension:
      | 'semantic'
      | 'requirement'
      | 'architectural'
      | 'implementation'
      | 'verification'
      | 'runtime'
      | 'source';
    score: number | 'unassessed';
    assessor: ActorRef;
    method: string;
    method_version: string;
    evidence: Digest1[];
    calibration_context?: string;
    timestamp: string;
    rationale: string;
  }
  /**
   * An uncertainty record, per ACT-1.0.md section 10.
   */
  export interface Uncertainty {
    id: UUID1;
    description: string;
    category:
      'known-unknown' | 'assumption' | 'speculation' | 'residual-risk' | 'human-input-required';
    source: ActorRef;
    introducing_transformation?: Digest1;
    /**
     * @minItems 1
     */
    affected_artifacts: [Digest1, ...Digest1[]];
    impact: 'low' | 'medium' | 'high' | 'critical';
    likelihood: 'low' | 'medium' | 'high';
    owner: ActorRef;
    status: 'open' | 'discharged' | 'accepted';
    resolution_criteria: string;
    inherited_from: UUID1[];
    resolving_evidence_or_decision?: Digest1 | null;
  }
}

// source: schemas/artifact/types/risk.schema.json
namespace NS_artifact_types_risk {
  /**
   * A recorded potential negative outcome.
   */
  export type Risk = ArtifactEnvelope & {
    artifact_type: 'Risk';
    data: {
      description: string;
      likelihood: 'low' | 'medium' | 'high';
      impact: 'low' | 'medium' | 'high' | 'critical';
      mitigation?: string;
    };
  };
  /**
   * Logical artifact identity, stable across revisions.
   */
  export type UUID = string;
  /**
   * Immutable version identity, the digest of this record's canonical bytes excluding signatures.
   */
  export type Digest = string;
  /**
   * A UUID, RFC 9562. ACT logical identities MUST use UUIDv7 (time-ordered) when newly generated; this pattern accepts any RFC 9562 UUID so that imported/legacy identifiers are not rejected on shape alone.
   */
  export type UUID1 = string;
  /**
   * A content or event digest in the form algorithm:encoded-value. Version 1.0 requires sha-256.
   */
  export type Digest1 = string;

  /**
   * The shared envelope every artifact-version payload extends, per ACT-1.0.md section 7.1 and PROMPT.md's Artifact Types requirements. Concrete type schemas under schemas/artifact/types/ set artifact_type to a fixed const and add a type-specific `data` object.
   */
  export interface ArtifactEnvelope {
    artifact_id: UUID;
    version_id: Digest;
    artifact_type: string;
    schema_version: string;
    protocol_version: 'act/1.0';
    authoring_actor: ActorRef;
    created_at_claim: string;
    content: ContentDescriptor;
    /**
     * Typed, many-to-many edges from this version to prior nodes it depends on. Empty only for a Genesis version.
     */
    lineage: {
      relation:
        'revises' | 'input-of' | 'output-of' | 'merges' | 'responds-to' | 'external-import-of';
      target_version_id: Digest1;
    }[];
    applicable_policy:
      | {
          policy_id: UUID1;
          policy_version: Digest1;
        }
      | {
          not_applicable: true;
          reason: string;
        };
    confidence_assessments: ConfidenceAssessment[];
    uncertainties: Uncertainty[];
    evidence_refs: Digest1[];
    /**
     * @minItems 1
     */
    signatures: [
      {
        key_id: string;
        algorithm: 'ed25519';
        signature: string;
      },
      ...{
        key_id: string;
        algorithm: 'ed25519';
        signature: string;
      }[],
    ];
    sensitivity: 'public' | 'internal' | 'confidential' | 'restricted';
    retention_policy_id: UUID1 | null;
    /**
     * Type-specific payload, defined by the concrete artifact type schema.
     */
    data: {};
    extensions?: {};
  }
  /**
   * A reference to the actor and signing key responsible for an event or attestation.
   */
  export interface ActorRef {
    actor_id: UUID1;
    /**
     * algorithm:fingerprint, e.g. ed25519:9f86d0...
     */
    key_id: string;
    display_name?: string;
  }
  /**
   * Describes artifact content separately from immutable event metadata, per ACT-1.0.md section 15.
   */
  export interface ContentDescriptor {
    media_type: string;
    byte_length: number;
    digest: Digest1;
    storage: {
      kind: 'inline' | 'filesystem' | 'database' | 'external';
      /**
       * base64-encoded content, present only when kind is inline
       */
      inline_value?: string;
      /**
       * opaque storage-provider locator, present when kind is filesystem/database/external
       */
      location?: string;
    };
    encryption?: {
      algorithm: 'none' | 'aes-256-gcm';
      key_id?: string;
      /**
       * base64-encoded nonce, required when algorithm is not none
       */
      nonce?: string;
    };
    sensitivity: 'public' | 'internal' | 'confidential' | 'restricted';
    retention_policy_id?: UUID1;
    availability_state: 'available' | 'redacted' | 'erased' | 'unavailable';
  }
  /**
   * A single-dimension, attributed confidence assessment, per ACT-1.0.md section 9.
   */
  export interface ConfidenceAssessment {
    dimension:
      | 'semantic'
      | 'requirement'
      | 'architectural'
      | 'implementation'
      | 'verification'
      | 'runtime'
      | 'source';
    score: number | 'unassessed';
    assessor: ActorRef;
    method: string;
    method_version: string;
    evidence: Digest1[];
    calibration_context?: string;
    timestamp: string;
    rationale: string;
  }
  /**
   * An uncertainty record, per ACT-1.0.md section 10.
   */
  export interface Uncertainty {
    id: UUID1;
    description: string;
    category:
      'known-unknown' | 'assumption' | 'speculation' | 'residual-risk' | 'human-input-required';
    source: ActorRef;
    introducing_transformation?: Digest1;
    /**
     * @minItems 1
     */
    affected_artifacts: [Digest1, ...Digest1[]];
    impact: 'low' | 'medium' | 'high' | 'critical';
    likelihood: 'low' | 'medium' | 'high';
    owner: ActorRef;
    status: 'open' | 'discharged' | 'accepted';
    resolution_criteria: string;
    inherited_from: UUID1[];
    resolving_evidence_or_decision?: Digest1 | null;
  }
}

// source: schemas/artifact/types/runtime-observation.schema.json
namespace NS_artifact_types_runtime_observation {
  /**
   * An observed runtime measurement.
   */
  export type RuntimeObservation = ArtifactEnvelope & {
    artifact_type: 'RuntimeObservation';
    data: {
      observed_at: string;
      metric_name: string;
      value: number;
      environment: string;
    };
  };
  /**
   * Logical artifact identity, stable across revisions.
   */
  export type UUID = string;
  /**
   * Immutable version identity, the digest of this record's canonical bytes excluding signatures.
   */
  export type Digest = string;
  /**
   * A UUID, RFC 9562. ACT logical identities MUST use UUIDv7 (time-ordered) when newly generated; this pattern accepts any RFC 9562 UUID so that imported/legacy identifiers are not rejected on shape alone.
   */
  export type UUID1 = string;
  /**
   * A content or event digest in the form algorithm:encoded-value. Version 1.0 requires sha-256.
   */
  export type Digest1 = string;

  /**
   * The shared envelope every artifact-version payload extends, per ACT-1.0.md section 7.1 and PROMPT.md's Artifact Types requirements. Concrete type schemas under schemas/artifact/types/ set artifact_type to a fixed const and add a type-specific `data` object.
   */
  export interface ArtifactEnvelope {
    artifact_id: UUID;
    version_id: Digest;
    artifact_type: string;
    schema_version: string;
    protocol_version: 'act/1.0';
    authoring_actor: ActorRef;
    created_at_claim: string;
    content: ContentDescriptor;
    /**
     * Typed, many-to-many edges from this version to prior nodes it depends on. Empty only for a Genesis version.
     */
    lineage: {
      relation:
        'revises' | 'input-of' | 'output-of' | 'merges' | 'responds-to' | 'external-import-of';
      target_version_id: Digest1;
    }[];
    applicable_policy:
      | {
          policy_id: UUID1;
          policy_version: Digest1;
        }
      | {
          not_applicable: true;
          reason: string;
        };
    confidence_assessments: ConfidenceAssessment[];
    uncertainties: Uncertainty[];
    evidence_refs: Digest1[];
    /**
     * @minItems 1
     */
    signatures: [
      {
        key_id: string;
        algorithm: 'ed25519';
        signature: string;
      },
      ...{
        key_id: string;
        algorithm: 'ed25519';
        signature: string;
      }[],
    ];
    sensitivity: 'public' | 'internal' | 'confidential' | 'restricted';
    retention_policy_id: UUID1 | null;
    /**
     * Type-specific payload, defined by the concrete artifact type schema.
     */
    data: {};
    extensions?: {};
  }
  /**
   * A reference to the actor and signing key responsible for an event or attestation.
   */
  export interface ActorRef {
    actor_id: UUID1;
    /**
     * algorithm:fingerprint, e.g. ed25519:9f86d0...
     */
    key_id: string;
    display_name?: string;
  }
  /**
   * Describes artifact content separately from immutable event metadata, per ACT-1.0.md section 15.
   */
  export interface ContentDescriptor {
    media_type: string;
    byte_length: number;
    digest: Digest1;
    storage: {
      kind: 'inline' | 'filesystem' | 'database' | 'external';
      /**
       * base64-encoded content, present only when kind is inline
       */
      inline_value?: string;
      /**
       * opaque storage-provider locator, present when kind is filesystem/database/external
       */
      location?: string;
    };
    encryption?: {
      algorithm: 'none' | 'aes-256-gcm';
      key_id?: string;
      /**
       * base64-encoded nonce, required when algorithm is not none
       */
      nonce?: string;
    };
    sensitivity: 'public' | 'internal' | 'confidential' | 'restricted';
    retention_policy_id?: UUID1;
    availability_state: 'available' | 'redacted' | 'erased' | 'unavailable';
  }
  /**
   * A single-dimension, attributed confidence assessment, per ACT-1.0.md section 9.
   */
  export interface ConfidenceAssessment {
    dimension:
      | 'semantic'
      | 'requirement'
      | 'architectural'
      | 'implementation'
      | 'verification'
      | 'runtime'
      | 'source';
    score: number | 'unassessed';
    assessor: ActorRef;
    method: string;
    method_version: string;
    evidence: Digest1[];
    calibration_context?: string;
    timestamp: string;
    rationale: string;
  }
  /**
   * An uncertainty record, per ACT-1.0.md section 10.
   */
  export interface Uncertainty {
    id: UUID1;
    description: string;
    category:
      'known-unknown' | 'assumption' | 'speculation' | 'residual-risk' | 'human-input-required';
    source: ActorRef;
    introducing_transformation?: Digest1;
    /**
     * @minItems 1
     */
    affected_artifacts: [Digest1, ...Digest1[]];
    impact: 'low' | 'medium' | 'high' | 'critical';
    likelihood: 'low' | 'medium' | 'high';
    owner: ActorRef;
    status: 'open' | 'discharged' | 'accepted';
    resolution_criteria: string;
    inherited_from: UUID1[];
    resolving_evidence_or_decision?: Digest1 | null;
  }
}

// source: schemas/artifact/types/source-code.schema.json
namespace NS_artifact_types_source_code {
  /**
   * A versioned source-code artifact.
   */
  export type SourceCode = ArtifactEnvelope & {
    artifact_type: 'SourceCode';
    data: {
      language: string;
      path: string;
      repository_ref?: string;
    };
  };
  /**
   * Logical artifact identity, stable across revisions.
   */
  export type UUID = string;
  /**
   * Immutable version identity, the digest of this record's canonical bytes excluding signatures.
   */
  export type Digest = string;
  /**
   * A UUID, RFC 9562. ACT logical identities MUST use UUIDv7 (time-ordered) when newly generated; this pattern accepts any RFC 9562 UUID so that imported/legacy identifiers are not rejected on shape alone.
   */
  export type UUID1 = string;
  /**
   * A content or event digest in the form algorithm:encoded-value. Version 1.0 requires sha-256.
   */
  export type Digest1 = string;

  /**
   * The shared envelope every artifact-version payload extends, per ACT-1.0.md section 7.1 and PROMPT.md's Artifact Types requirements. Concrete type schemas under schemas/artifact/types/ set artifact_type to a fixed const and add a type-specific `data` object.
   */
  export interface ArtifactEnvelope {
    artifact_id: UUID;
    version_id: Digest;
    artifact_type: string;
    schema_version: string;
    protocol_version: 'act/1.0';
    authoring_actor: ActorRef;
    created_at_claim: string;
    content: ContentDescriptor;
    /**
     * Typed, many-to-many edges from this version to prior nodes it depends on. Empty only for a Genesis version.
     */
    lineage: {
      relation:
        'revises' | 'input-of' | 'output-of' | 'merges' | 'responds-to' | 'external-import-of';
      target_version_id: Digest1;
    }[];
    applicable_policy:
      | {
          policy_id: UUID1;
          policy_version: Digest1;
        }
      | {
          not_applicable: true;
          reason: string;
        };
    confidence_assessments: ConfidenceAssessment[];
    uncertainties: Uncertainty[];
    evidence_refs: Digest1[];
    /**
     * @minItems 1
     */
    signatures: [
      {
        key_id: string;
        algorithm: 'ed25519';
        signature: string;
      },
      ...{
        key_id: string;
        algorithm: 'ed25519';
        signature: string;
      }[],
    ];
    sensitivity: 'public' | 'internal' | 'confidential' | 'restricted';
    retention_policy_id: UUID1 | null;
    /**
     * Type-specific payload, defined by the concrete artifact type schema.
     */
    data: {};
    extensions?: {};
  }
  /**
   * A reference to the actor and signing key responsible for an event or attestation.
   */
  export interface ActorRef {
    actor_id: UUID1;
    /**
     * algorithm:fingerprint, e.g. ed25519:9f86d0...
     */
    key_id: string;
    display_name?: string;
  }
  /**
   * Describes artifact content separately from immutable event metadata, per ACT-1.0.md section 15.
   */
  export interface ContentDescriptor {
    media_type: string;
    byte_length: number;
    digest: Digest1;
    storage: {
      kind: 'inline' | 'filesystem' | 'database' | 'external';
      /**
       * base64-encoded content, present only when kind is inline
       */
      inline_value?: string;
      /**
       * opaque storage-provider locator, present when kind is filesystem/database/external
       */
      location?: string;
    };
    encryption?: {
      algorithm: 'none' | 'aes-256-gcm';
      key_id?: string;
      /**
       * base64-encoded nonce, required when algorithm is not none
       */
      nonce?: string;
    };
    sensitivity: 'public' | 'internal' | 'confidential' | 'restricted';
    retention_policy_id?: UUID1;
    availability_state: 'available' | 'redacted' | 'erased' | 'unavailable';
  }
  /**
   * A single-dimension, attributed confidence assessment, per ACT-1.0.md section 9.
   */
  export interface ConfidenceAssessment {
    dimension:
      | 'semantic'
      | 'requirement'
      | 'architectural'
      | 'implementation'
      | 'verification'
      | 'runtime'
      | 'source';
    score: number | 'unassessed';
    assessor: ActorRef;
    method: string;
    method_version: string;
    evidence: Digest1[];
    calibration_context?: string;
    timestamp: string;
    rationale: string;
  }
  /**
   * An uncertainty record, per ACT-1.0.md section 10.
   */
  export interface Uncertainty {
    id: UUID1;
    description: string;
    category:
      'known-unknown' | 'assumption' | 'speculation' | 'residual-risk' | 'human-input-required';
    source: ActorRef;
    introducing_transformation?: Digest1;
    /**
     * @minItems 1
     */
    affected_artifacts: [Digest1, ...Digest1[]];
    impact: 'low' | 'medium' | 'high' | 'critical';
    likelihood: 'low' | 'medium' | 'high';
    owner: ActorRef;
    status: 'open' | 'discharged' | 'accepted';
    resolution_criteria: string;
    inherited_from: UUID1[];
    resolving_evidence_or_decision?: Digest1 | null;
  }
}

// source: schemas/artifact/types/task.schema.json
namespace NS_artifact_types_task {
  /**
   * A unit of planned or completed work.
   */
  export type Task = ArtifactEnvelope & {
    artifact_type: 'Task';
    data: {
      title: string;
      description: string;
      status: 'open' | 'in_progress' | 'done' | 'blocked';
      assignee?: ActorRef;
    };
  };
  /**
   * Logical artifact identity, stable across revisions.
   */
  export type UUID = string;
  /**
   * Immutable version identity, the digest of this record's canonical bytes excluding signatures.
   */
  export type Digest = string;
  /**
   * A UUID, RFC 9562. ACT logical identities MUST use UUIDv7 (time-ordered) when newly generated; this pattern accepts any RFC 9562 UUID so that imported/legacy identifiers are not rejected on shape alone.
   */
  export type UUID1 = string;
  /**
   * A content or event digest in the form algorithm:encoded-value. Version 1.0 requires sha-256.
   */
  export type Digest1 = string;

  /**
   * The shared envelope every artifact-version payload extends, per ACT-1.0.md section 7.1 and PROMPT.md's Artifact Types requirements. Concrete type schemas under schemas/artifact/types/ set artifact_type to a fixed const and add a type-specific `data` object.
   */
  export interface ArtifactEnvelope {
    artifact_id: UUID;
    version_id: Digest;
    artifact_type: string;
    schema_version: string;
    protocol_version: 'act/1.0';
    authoring_actor: ActorRef;
    created_at_claim: string;
    content: ContentDescriptor;
    /**
     * Typed, many-to-many edges from this version to prior nodes it depends on. Empty only for a Genesis version.
     */
    lineage: {
      relation:
        'revises' | 'input-of' | 'output-of' | 'merges' | 'responds-to' | 'external-import-of';
      target_version_id: Digest1;
    }[];
    applicable_policy:
      | {
          policy_id: UUID1;
          policy_version: Digest1;
        }
      | {
          not_applicable: true;
          reason: string;
        };
    confidence_assessments: ConfidenceAssessment[];
    uncertainties: Uncertainty[];
    evidence_refs: Digest1[];
    /**
     * @minItems 1
     */
    signatures: [
      {
        key_id: string;
        algorithm: 'ed25519';
        signature: string;
      },
      ...{
        key_id: string;
        algorithm: 'ed25519';
        signature: string;
      }[],
    ];
    sensitivity: 'public' | 'internal' | 'confidential' | 'restricted';
    retention_policy_id: UUID1 | null;
    /**
     * Type-specific payload, defined by the concrete artifact type schema.
     */
    data: {};
    extensions?: {};
  }
  /**
   * A reference to the actor and signing key responsible for an event or attestation.
   */
  export interface ActorRef {
    actor_id: UUID1;
    /**
     * algorithm:fingerprint, e.g. ed25519:9f86d0...
     */
    key_id: string;
    display_name?: string;
  }
  /**
   * Describes artifact content separately from immutable event metadata, per ACT-1.0.md section 15.
   */
  export interface ContentDescriptor {
    media_type: string;
    byte_length: number;
    digest: Digest1;
    storage: {
      kind: 'inline' | 'filesystem' | 'database' | 'external';
      /**
       * base64-encoded content, present only when kind is inline
       */
      inline_value?: string;
      /**
       * opaque storage-provider locator, present when kind is filesystem/database/external
       */
      location?: string;
    };
    encryption?: {
      algorithm: 'none' | 'aes-256-gcm';
      key_id?: string;
      /**
       * base64-encoded nonce, required when algorithm is not none
       */
      nonce?: string;
    };
    sensitivity: 'public' | 'internal' | 'confidential' | 'restricted';
    retention_policy_id?: UUID1;
    availability_state: 'available' | 'redacted' | 'erased' | 'unavailable';
  }
  /**
   * A single-dimension, attributed confidence assessment, per ACT-1.0.md section 9.
   */
  export interface ConfidenceAssessment {
    dimension:
      | 'semantic'
      | 'requirement'
      | 'architectural'
      | 'implementation'
      | 'verification'
      | 'runtime'
      | 'source';
    score: number | 'unassessed';
    assessor: ActorRef;
    method: string;
    method_version: string;
    evidence: Digest1[];
    calibration_context?: string;
    timestamp: string;
    rationale: string;
  }
  /**
   * An uncertainty record, per ACT-1.0.md section 10.
   */
  export interface Uncertainty {
    id: UUID1;
    description: string;
    category:
      'known-unknown' | 'assumption' | 'speculation' | 'residual-risk' | 'human-input-required';
    source: ActorRef;
    introducing_transformation?: Digest1;
    /**
     * @minItems 1
     */
    affected_artifacts: [Digest1, ...Digest1[]];
    impact: 'low' | 'medium' | 'high' | 'critical';
    likelihood: 'low' | 'medium' | 'high';
    owner: ActorRef;
    status: 'open' | 'discharged' | 'accepted';
    resolution_criteria: string;
    inherited_from: UUID1[];
    resolving_evidence_or_decision?: Digest1 | null;
  }
}

// source: schemas/artifact/types/test.schema.json
namespace NS_artifact_types_test {
  /**
   * A test definition or run record.
   */
  export type Test = ArtifactEnvelope & {
    artifact_type: 'Test';
    data: {
      name: string;
      framework: string;
      status: 'passing' | 'failing' | 'skipped';
      coverage_percent?: number;
    };
  };
  /**
   * Logical artifact identity, stable across revisions.
   */
  export type UUID = string;
  /**
   * Immutable version identity, the digest of this record's canonical bytes excluding signatures.
   */
  export type Digest = string;
  /**
   * A UUID, RFC 9562. ACT logical identities MUST use UUIDv7 (time-ordered) when newly generated; this pattern accepts any RFC 9562 UUID so that imported/legacy identifiers are not rejected on shape alone.
   */
  export type UUID1 = string;
  /**
   * A content or event digest in the form algorithm:encoded-value. Version 1.0 requires sha-256.
   */
  export type Digest1 = string;

  /**
   * The shared envelope every artifact-version payload extends, per ACT-1.0.md section 7.1 and PROMPT.md's Artifact Types requirements. Concrete type schemas under schemas/artifact/types/ set artifact_type to a fixed const and add a type-specific `data` object.
   */
  export interface ArtifactEnvelope {
    artifact_id: UUID;
    version_id: Digest;
    artifact_type: string;
    schema_version: string;
    protocol_version: 'act/1.0';
    authoring_actor: ActorRef;
    created_at_claim: string;
    content: ContentDescriptor;
    /**
     * Typed, many-to-many edges from this version to prior nodes it depends on. Empty only for a Genesis version.
     */
    lineage: {
      relation:
        'revises' | 'input-of' | 'output-of' | 'merges' | 'responds-to' | 'external-import-of';
      target_version_id: Digest1;
    }[];
    applicable_policy:
      | {
          policy_id: UUID1;
          policy_version: Digest1;
        }
      | {
          not_applicable: true;
          reason: string;
        };
    confidence_assessments: ConfidenceAssessment[];
    uncertainties: Uncertainty[];
    evidence_refs: Digest1[];
    /**
     * @minItems 1
     */
    signatures: [
      {
        key_id: string;
        algorithm: 'ed25519';
        signature: string;
      },
      ...{
        key_id: string;
        algorithm: 'ed25519';
        signature: string;
      }[],
    ];
    sensitivity: 'public' | 'internal' | 'confidential' | 'restricted';
    retention_policy_id: UUID1 | null;
    /**
     * Type-specific payload, defined by the concrete artifact type schema.
     */
    data: {};
    extensions?: {};
  }
  /**
   * A reference to the actor and signing key responsible for an event or attestation.
   */
  export interface ActorRef {
    actor_id: UUID1;
    /**
     * algorithm:fingerprint, e.g. ed25519:9f86d0...
     */
    key_id: string;
    display_name?: string;
  }
  /**
   * Describes artifact content separately from immutable event metadata, per ACT-1.0.md section 15.
   */
  export interface ContentDescriptor {
    media_type: string;
    byte_length: number;
    digest: Digest1;
    storage: {
      kind: 'inline' | 'filesystem' | 'database' | 'external';
      /**
       * base64-encoded content, present only when kind is inline
       */
      inline_value?: string;
      /**
       * opaque storage-provider locator, present when kind is filesystem/database/external
       */
      location?: string;
    };
    encryption?: {
      algorithm: 'none' | 'aes-256-gcm';
      key_id?: string;
      /**
       * base64-encoded nonce, required when algorithm is not none
       */
      nonce?: string;
    };
    sensitivity: 'public' | 'internal' | 'confidential' | 'restricted';
    retention_policy_id?: UUID1;
    availability_state: 'available' | 'redacted' | 'erased' | 'unavailable';
  }
  /**
   * A single-dimension, attributed confidence assessment, per ACT-1.0.md section 9.
   */
  export interface ConfidenceAssessment {
    dimension:
      | 'semantic'
      | 'requirement'
      | 'architectural'
      | 'implementation'
      | 'verification'
      | 'runtime'
      | 'source';
    score: number | 'unassessed';
    assessor: ActorRef;
    method: string;
    method_version: string;
    evidence: Digest1[];
    calibration_context?: string;
    timestamp: string;
    rationale: string;
  }
  /**
   * An uncertainty record, per ACT-1.0.md section 10.
   */
  export interface Uncertainty {
    id: UUID1;
    description: string;
    category:
      'known-unknown' | 'assumption' | 'speculation' | 'residual-risk' | 'human-input-required';
    source: ActorRef;
    introducing_transformation?: Digest1;
    /**
     * @minItems 1
     */
    affected_artifacts: [Digest1, ...Digest1[]];
    impact: 'low' | 'medium' | 'high' | 'critical';
    likelihood: 'low' | 'medium' | 'high';
    owner: ActorRef;
    status: 'open' | 'discharged' | 'accepted';
    resolution_criteria: string;
    inherited_from: UUID1[];
    resolving_evidence_or_decision?: Digest1 | null;
  }
}

// source: schemas/artifact/types/tool-invocation.schema.json
namespace NS_artifact_types_tool_invocation {
  /**
   * A recorded invocation of a tool by an actor, redactable.
   */
  export type ToolInvocation = ArtifactEnvelope & {
    artifact_type: 'ToolInvocation';
    data: {
      tool_name: string;
      arguments_digest: Digest1;
      result_digest?: Digest1;
      redacted: boolean;
    };
  };
  /**
   * Logical artifact identity, stable across revisions.
   */
  export type UUID = string;
  /**
   * Immutable version identity, the digest of this record's canonical bytes excluding signatures.
   */
  export type Digest = string;
  /**
   * A UUID, RFC 9562. ACT logical identities MUST use UUIDv7 (time-ordered) when newly generated; this pattern accepts any RFC 9562 UUID so that imported/legacy identifiers are not rejected on shape alone.
   */
  export type UUID1 = string;
  /**
   * A content or event digest in the form algorithm:encoded-value. Version 1.0 requires sha-256.
   */
  export type Digest1 = string;

  /**
   * The shared envelope every artifact-version payload extends, per ACT-1.0.md section 7.1 and PROMPT.md's Artifact Types requirements. Concrete type schemas under schemas/artifact/types/ set artifact_type to a fixed const and add a type-specific `data` object.
   */
  export interface ArtifactEnvelope {
    artifact_id: UUID;
    version_id: Digest;
    artifact_type: string;
    schema_version: string;
    protocol_version: 'act/1.0';
    authoring_actor: ActorRef;
    created_at_claim: string;
    content: ContentDescriptor;
    /**
     * Typed, many-to-many edges from this version to prior nodes it depends on. Empty only for a Genesis version.
     */
    lineage: {
      relation:
        'revises' | 'input-of' | 'output-of' | 'merges' | 'responds-to' | 'external-import-of';
      target_version_id: Digest1;
    }[];
    applicable_policy:
      | {
          policy_id: UUID1;
          policy_version: Digest1;
        }
      | {
          not_applicable: true;
          reason: string;
        };
    confidence_assessments: ConfidenceAssessment[];
    uncertainties: Uncertainty[];
    evidence_refs: Digest1[];
    /**
     * @minItems 1
     */
    signatures: [
      {
        key_id: string;
        algorithm: 'ed25519';
        signature: string;
      },
      ...{
        key_id: string;
        algorithm: 'ed25519';
        signature: string;
      }[],
    ];
    sensitivity: 'public' | 'internal' | 'confidential' | 'restricted';
    retention_policy_id: UUID1 | null;
    /**
     * Type-specific payload, defined by the concrete artifact type schema.
     */
    data: {};
    extensions?: {};
  }
  /**
   * A reference to the actor and signing key responsible for an event or attestation.
   */
  export interface ActorRef {
    actor_id: UUID1;
    /**
     * algorithm:fingerprint, e.g. ed25519:9f86d0...
     */
    key_id: string;
    display_name?: string;
  }
  /**
   * Describes artifact content separately from immutable event metadata, per ACT-1.0.md section 15.
   */
  export interface ContentDescriptor {
    media_type: string;
    byte_length: number;
    digest: Digest1;
    storage: {
      kind: 'inline' | 'filesystem' | 'database' | 'external';
      /**
       * base64-encoded content, present only when kind is inline
       */
      inline_value?: string;
      /**
       * opaque storage-provider locator, present when kind is filesystem/database/external
       */
      location?: string;
    };
    encryption?: {
      algorithm: 'none' | 'aes-256-gcm';
      key_id?: string;
      /**
       * base64-encoded nonce, required when algorithm is not none
       */
      nonce?: string;
    };
    sensitivity: 'public' | 'internal' | 'confidential' | 'restricted';
    retention_policy_id?: UUID1;
    availability_state: 'available' | 'redacted' | 'erased' | 'unavailable';
  }
  /**
   * A single-dimension, attributed confidence assessment, per ACT-1.0.md section 9.
   */
  export interface ConfidenceAssessment {
    dimension:
      | 'semantic'
      | 'requirement'
      | 'architectural'
      | 'implementation'
      | 'verification'
      | 'runtime'
      | 'source';
    score: number | 'unassessed';
    assessor: ActorRef;
    method: string;
    method_version: string;
    evidence: Digest1[];
    calibration_context?: string;
    timestamp: string;
    rationale: string;
  }
  /**
   * An uncertainty record, per ACT-1.0.md section 10.
   */
  export interface Uncertainty {
    id: UUID1;
    description: string;
    category:
      'known-unknown' | 'assumption' | 'speculation' | 'residual-risk' | 'human-input-required';
    source: ActorRef;
    introducing_transformation?: Digest1;
    /**
     * @minItems 1
     */
    affected_artifacts: [Digest1, ...Digest1[]];
    impact: 'low' | 'medium' | 'high' | 'critical';
    likelihood: 'low' | 'medium' | 'high';
    owner: ActorRef;
    status: 'open' | 'discharged' | 'accepted';
    resolution_criteria: string;
    inherited_from: UUID1[];
    resolving_evidence_or_decision?: Digest1 | null;
  }
}

// source: schemas/artifact/types/user-feedback.schema.json
namespace NS_artifact_types_user_feedback {
  /**
   * Feedback from a human user.
   */
  export type UserFeedback = ArtifactEnvelope & {
    artifact_type: 'UserFeedback';
    data: {
      rating?: number;
      comment: string;
      sentiment?: 'positive' | 'neutral' | 'negative';
    };
  };
  /**
   * Logical artifact identity, stable across revisions.
   */
  export type UUID = string;
  /**
   * Immutable version identity, the digest of this record's canonical bytes excluding signatures.
   */
  export type Digest = string;
  /**
   * A UUID, RFC 9562. ACT logical identities MUST use UUIDv7 (time-ordered) when newly generated; this pattern accepts any RFC 9562 UUID so that imported/legacy identifiers are not rejected on shape alone.
   */
  export type UUID1 = string;
  /**
   * A content or event digest in the form algorithm:encoded-value. Version 1.0 requires sha-256.
   */
  export type Digest1 = string;

  /**
   * The shared envelope every artifact-version payload extends, per ACT-1.0.md section 7.1 and PROMPT.md's Artifact Types requirements. Concrete type schemas under schemas/artifact/types/ set artifact_type to a fixed const and add a type-specific `data` object.
   */
  export interface ArtifactEnvelope {
    artifact_id: UUID;
    version_id: Digest;
    artifact_type: string;
    schema_version: string;
    protocol_version: 'act/1.0';
    authoring_actor: ActorRef;
    created_at_claim: string;
    content: ContentDescriptor;
    /**
     * Typed, many-to-many edges from this version to prior nodes it depends on. Empty only for a Genesis version.
     */
    lineage: {
      relation:
        'revises' | 'input-of' | 'output-of' | 'merges' | 'responds-to' | 'external-import-of';
      target_version_id: Digest1;
    }[];
    applicable_policy:
      | {
          policy_id: UUID1;
          policy_version: Digest1;
        }
      | {
          not_applicable: true;
          reason: string;
        };
    confidence_assessments: ConfidenceAssessment[];
    uncertainties: Uncertainty[];
    evidence_refs: Digest1[];
    /**
     * @minItems 1
     */
    signatures: [
      {
        key_id: string;
        algorithm: 'ed25519';
        signature: string;
      },
      ...{
        key_id: string;
        algorithm: 'ed25519';
        signature: string;
      }[],
    ];
    sensitivity: 'public' | 'internal' | 'confidential' | 'restricted';
    retention_policy_id: UUID1 | null;
    /**
     * Type-specific payload, defined by the concrete artifact type schema.
     */
    data: {};
    extensions?: {};
  }
  /**
   * A reference to the actor and signing key responsible for an event or attestation.
   */
  export interface ActorRef {
    actor_id: UUID1;
    /**
     * algorithm:fingerprint, e.g. ed25519:9f86d0...
     */
    key_id: string;
    display_name?: string;
  }
  /**
   * Describes artifact content separately from immutable event metadata, per ACT-1.0.md section 15.
   */
  export interface ContentDescriptor {
    media_type: string;
    byte_length: number;
    digest: Digest1;
    storage: {
      kind: 'inline' | 'filesystem' | 'database' | 'external';
      /**
       * base64-encoded content, present only when kind is inline
       */
      inline_value?: string;
      /**
       * opaque storage-provider locator, present when kind is filesystem/database/external
       */
      location?: string;
    };
    encryption?: {
      algorithm: 'none' | 'aes-256-gcm';
      key_id?: string;
      /**
       * base64-encoded nonce, required when algorithm is not none
       */
      nonce?: string;
    };
    sensitivity: 'public' | 'internal' | 'confidential' | 'restricted';
    retention_policy_id?: UUID1;
    availability_state: 'available' | 'redacted' | 'erased' | 'unavailable';
  }
  /**
   * A single-dimension, attributed confidence assessment, per ACT-1.0.md section 9.
   */
  export interface ConfidenceAssessment {
    dimension:
      | 'semantic'
      | 'requirement'
      | 'architectural'
      | 'implementation'
      | 'verification'
      | 'runtime'
      | 'source';
    score: number | 'unassessed';
    assessor: ActorRef;
    method: string;
    method_version: string;
    evidence: Digest1[];
    calibration_context?: string;
    timestamp: string;
    rationale: string;
  }
  /**
   * An uncertainty record, per ACT-1.0.md section 10.
   */
  export interface Uncertainty {
    id: UUID1;
    description: string;
    category:
      'known-unknown' | 'assumption' | 'speculation' | 'residual-risk' | 'human-input-required';
    source: ActorRef;
    introducing_transformation?: Digest1;
    /**
     * @minItems 1
     */
    affected_artifacts: [Digest1, ...Digest1[]];
    impact: 'low' | 'medium' | 'high' | 'critical';
    likelihood: 'low' | 'medium' | 'high';
    owner: ActorRef;
    status: 'open' | 'discharged' | 'accepted';
    resolution_criteria: string;
    inherited_from: UUID1[];
    resolving_evidence_or_decision?: Digest1 | null;
  }
}

// source: schemas/artifact/types/verification-report.schema.json
namespace NS_artifact_types_verification_report {
  /**
   * A verification result (ACT-1.0.md section 11.3).
   */
  export type VerificationReport = ArtifactEnvelope & {
    artifact_type: 'VerificationReport';
    data: {
      result: 'pass' | 'fail' | 'inconclusive';
      verifier: ActorRef;
      method: string;
      method_version: string;
      subject_digest: Digest1;
      execution_environment: string;
      confidence?: ConfidenceAssessment;
      limitations: string;
    };
  };
  /**
   * Logical artifact identity, stable across revisions.
   */
  export type UUID = string;
  /**
   * Immutable version identity, the digest of this record's canonical bytes excluding signatures.
   */
  export type Digest = string;
  /**
   * A UUID, RFC 9562. ACT logical identities MUST use UUIDv7 (time-ordered) when newly generated; this pattern accepts any RFC 9562 UUID so that imported/legacy identifiers are not rejected on shape alone.
   */
  export type UUID1 = string;
  /**
   * A content or event digest in the form algorithm:encoded-value. Version 1.0 requires sha-256.
   */
  export type Digest1 = string;

  /**
   * The shared envelope every artifact-version payload extends, per ACT-1.0.md section 7.1 and PROMPT.md's Artifact Types requirements. Concrete type schemas under schemas/artifact/types/ set artifact_type to a fixed const and add a type-specific `data` object.
   */
  export interface ArtifactEnvelope {
    artifact_id: UUID;
    version_id: Digest;
    artifact_type: string;
    schema_version: string;
    protocol_version: 'act/1.0';
    authoring_actor: ActorRef;
    created_at_claim: string;
    content: ContentDescriptor;
    /**
     * Typed, many-to-many edges from this version to prior nodes it depends on. Empty only for a Genesis version.
     */
    lineage: {
      relation:
        'revises' | 'input-of' | 'output-of' | 'merges' | 'responds-to' | 'external-import-of';
      target_version_id: Digest1;
    }[];
    applicable_policy:
      | {
          policy_id: UUID1;
          policy_version: Digest1;
        }
      | {
          not_applicable: true;
          reason: string;
        };
    confidence_assessments: ConfidenceAssessment[];
    uncertainties: Uncertainty[];
    evidence_refs: Digest1[];
    /**
     * @minItems 1
     */
    signatures: [
      {
        key_id: string;
        algorithm: 'ed25519';
        signature: string;
      },
      ...{
        key_id: string;
        algorithm: 'ed25519';
        signature: string;
      }[],
    ];
    sensitivity: 'public' | 'internal' | 'confidential' | 'restricted';
    retention_policy_id: UUID1 | null;
    /**
     * Type-specific payload, defined by the concrete artifact type schema.
     */
    data: {};
    extensions?: {};
  }
  /**
   * A reference to the actor and signing key responsible for an event or attestation.
   */
  export interface ActorRef {
    actor_id: UUID1;
    /**
     * algorithm:fingerprint, e.g. ed25519:9f86d0...
     */
    key_id: string;
    display_name?: string;
  }
  /**
   * Describes artifact content separately from immutable event metadata, per ACT-1.0.md section 15.
   */
  export interface ContentDescriptor {
    media_type: string;
    byte_length: number;
    digest: Digest1;
    storage: {
      kind: 'inline' | 'filesystem' | 'database' | 'external';
      /**
       * base64-encoded content, present only when kind is inline
       */
      inline_value?: string;
      /**
       * opaque storage-provider locator, present when kind is filesystem/database/external
       */
      location?: string;
    };
    encryption?: {
      algorithm: 'none' | 'aes-256-gcm';
      key_id?: string;
      /**
       * base64-encoded nonce, required when algorithm is not none
       */
      nonce?: string;
    };
    sensitivity: 'public' | 'internal' | 'confidential' | 'restricted';
    retention_policy_id?: UUID1;
    availability_state: 'available' | 'redacted' | 'erased' | 'unavailable';
  }
  /**
   * A single-dimension, attributed confidence assessment, per ACT-1.0.md section 9.
   */
  export interface ConfidenceAssessment {
    dimension:
      | 'semantic'
      | 'requirement'
      | 'architectural'
      | 'implementation'
      | 'verification'
      | 'runtime'
      | 'source';
    score: number | 'unassessed';
    assessor: ActorRef;
    method: string;
    method_version: string;
    evidence: Digest1[];
    calibration_context?: string;
    timestamp: string;
    rationale: string;
  }
  /**
   * An uncertainty record, per ACT-1.0.md section 10.
   */
  export interface Uncertainty {
    id: UUID1;
    description: string;
    category:
      'known-unknown' | 'assumption' | 'speculation' | 'residual-risk' | 'human-input-required';
    source: ActorRef;
    introducing_transformation?: Digest1;
    /**
     * @minItems 1
     */
    affected_artifacts: [Digest1, ...Digest1[]];
    impact: 'low' | 'medium' | 'high' | 'critical';
    likelihood: 'low' | 'medium' | 'high';
    owner: ActorRef;
    status: 'open' | 'discharged' | 'accepted';
    resolution_criteria: string;
    inherited_from: UUID1[];
    resolving_evidence_or_decision?: Digest1 | null;
  }
}

// source: schemas/challenge/challenge.schema.json
namespace NS_challenge_challenge {
  /**
   * A UUID, RFC 9562. ACT logical identities MUST use UUIDv7 (time-ordered) when newly generated; this pattern accepts any RFC 9562 UUID so that imported/legacy identifiers are not rejected on shape alone.
   */
  export type UUID = string;
  /**
   * A content or event digest in the form algorithm:encoded-value. Version 1.0 requires sha-256.
   */
  export type Digest = string;

  /**
   * A first-class signed dispute attestation. See ACT-1.0.md section 11.4.
   */
  export interface Challenge {
    challenge_id: UUID;
    disputed_claim: Digest;
    challenger: ActorRef;
    grounds: string;
    evidence: Digest[];
    requested_remedy: string;
    status: 'open' | 'resolved_upheld' | 'resolved_rejected' | 'resolved_remedied';
    resolution?: {
      resolved_by: ActorRef;
      resolved_at: string;
      rationale: string;
      remediating_event_id?: Digest;
    };
    issued_at: string;
    signature: {
      key_id: string;
      algorithm: 'ed25519';
      signature: string;
    };
  }
  /**
   * A reference to the actor and signing key responsible for an event or attestation.
   */
  export interface ActorRef {
    actor_id: UUID;
    /**
     * algorithm:fingerprint, e.g. ed25519:9f86d0...
     */
    key_id: string;
    display_name?: string;
  }
}

// source: schemas/common/actor-ref.schema.json
namespace NS_common_actor_ref {
  /**
   * A UUID, RFC 9562. ACT logical identities MUST use UUIDv7 (time-ordered) when newly generated; this pattern accepts any RFC 9562 UUID so that imported/legacy identifiers are not rejected on shape alone.
   */
  export type UUID = string;

  /**
   * A reference to the actor and signing key responsible for an event or attestation.
   */
  export interface ActorRef {
    actor_id: UUID;
    /**
     * algorithm:fingerprint, e.g. ed25519:9f86d0...
     */
    key_id: string;
    display_name?: string;
  }
}

// source: schemas/common/confidence-assessment.schema.json
namespace NS_common_confidence_assessment {
  /**
   * A UUID, RFC 9562. ACT logical identities MUST use UUIDv7 (time-ordered) when newly generated; this pattern accepts any RFC 9562 UUID so that imported/legacy identifiers are not rejected on shape alone.
   */
  export type UUID = string;
  /**
   * A content or event digest in the form algorithm:encoded-value. Version 1.0 requires sha-256.
   */
  export type Digest = string;

  /**
   * A single-dimension, attributed confidence assessment, per ACT-1.0.md section 9.
   */
  export interface ConfidenceAssessment {
    dimension:
      | 'semantic'
      | 'requirement'
      | 'architectural'
      | 'implementation'
      | 'verification'
      | 'runtime'
      | 'source';
    score: number | 'unassessed';
    assessor: ActorRef;
    method: string;
    method_version: string;
    evidence: Digest[];
    calibration_context?: string;
    timestamp: string;
    rationale: string;
  }
  /**
   * A reference to the actor and signing key responsible for an event or attestation.
   */
  export interface ActorRef {
    actor_id: UUID;
    /**
     * algorithm:fingerprint, e.g. ed25519:9f86d0...
     */
    key_id: string;
    display_name?: string;
  }
}

// source: schemas/common/content-descriptor.schema.json
namespace NS_common_content_descriptor {
  /**
   * A content or event digest in the form algorithm:encoded-value. Version 1.0 requires sha-256.
   */
  export type Digest = string;
  /**
   * A UUID, RFC 9562. ACT logical identities MUST use UUIDv7 (time-ordered) when newly generated; this pattern accepts any RFC 9562 UUID so that imported/legacy identifiers are not rejected on shape alone.
   */
  export type UUID = string;

  /**
   * Describes artifact content separately from immutable event metadata, per ACT-1.0.md section 15.
   */
  export interface ContentDescriptor {
    media_type: string;
    byte_length: number;
    digest: Digest;
    storage: {
      kind: 'inline' | 'filesystem' | 'database' | 'external';
      /**
       * base64-encoded content, present only when kind is inline
       */
      inline_value?: string;
      /**
       * opaque storage-provider locator, present when kind is filesystem/database/external
       */
      location?: string;
    };
    encryption?: {
      algorithm: 'none' | 'aes-256-gcm';
      key_id?: string;
      /**
       * base64-encoded nonce, required when algorithm is not none
       */
      nonce?: string;
    };
    sensitivity: 'public' | 'internal' | 'confidential' | 'restricted';
    retention_policy_id?: UUID;
    availability_state: 'available' | 'redacted' | 'erased' | 'unavailable';
  }
}

// source: schemas/common/digest.schema.json
namespace NS_common_digest {
  /**
   * A content or event digest in the form algorithm:encoded-value. Version 1.0 requires sha-256.
   */
  export type Digest = string;
}

// source: schemas/common/uncertainty.schema.json
namespace NS_common_uncertainty {
  /**
   * A UUID, RFC 9562. ACT logical identities MUST use UUIDv7 (time-ordered) when newly generated; this pattern accepts any RFC 9562 UUID so that imported/legacy identifiers are not rejected on shape alone.
   */
  export type UUID = string;
  /**
   * A content or event digest in the form algorithm:encoded-value. Version 1.0 requires sha-256.
   */
  export type Digest = string;

  /**
   * An uncertainty record, per ACT-1.0.md section 10.
   */
  export interface Uncertainty {
    id: UUID;
    description: string;
    category:
      'known-unknown' | 'assumption' | 'speculation' | 'residual-risk' | 'human-input-required';
    source: ActorRef;
    introducing_transformation?: Digest;
    /**
     * @minItems 1
     */
    affected_artifacts: [Digest, ...Digest[]];
    impact: 'low' | 'medium' | 'high' | 'critical';
    likelihood: 'low' | 'medium' | 'high';
    owner: ActorRef;
    status: 'open' | 'discharged' | 'accepted';
    resolution_criteria: string;
    inherited_from: UUID[];
    resolving_evidence_or_decision?: Digest | null;
  }
  /**
   * A reference to the actor and signing key responsible for an event or attestation.
   */
  export interface ActorRef {
    actor_id: UUID;
    /**
     * algorithm:fingerprint, e.g. ed25519:9f86d0...
     */
    key_id: string;
    display_name?: string;
  }
}

// source: schemas/common/uuid.schema.json
namespace NS_common_uuid {
  /**
   * A UUID, RFC 9562. ACT logical identities MUST use UUIDv7 (time-ordered) when newly generated; this pattern accepts any RFC 9562 UUID so that imported/legacy identifiers are not rejected on shape alone.
   */
  export type UUID = string;
}

// source: schemas/envelope/signed-envelope.schema.json
namespace NS_envelope_signed_envelope {
  /**
   * The unsigned event payload, in-band for local/embedded use. Wire transport MAY instead base64-encode this per raw DSSE; both representations MUST hash identically once canonicalized.
   */
  export type UnsignedEvent = {
    [k: string]: unknown;
  } & {
    protocol_version: 'act/1.0';
    event_type: EventType;
    occurred_at: string;
    actor: ActorRef;
    tenant:
      | string
      | {
          not_applicable: true;
          reason: string;
        };
    subject: {
      kind: 'artifact' | 'transformation' | 'attestation' | 'policy' | 'evidence';
      artifact_id?: UUID;
      version_id?: Digest;
      artifact_type?: string;
    };
    causal_parents: {
      event_id: Digest;
      relation?: 'input' | 'output' | 'revision-of' | 'merge-of' | 'approval-of' | 'response-to';
    }[];
    content_descriptors: ContentDescriptor[];
    policy_context:
      | {
          policy_id: UUID;
          policy_version: Digest;
        }
      | {
          not_applicable: true;
          reason: string;
        };
    payload: {};
    extensions: {};
  };
  /**
   * The closed enumeration of ACT 1.0 event types. An implementation MUST reject an unrecognized value rather than reinterpret it (ACT-1.0.md section 16.4).
   */
  export type EventType =
    | 'genesis'
    | 'external_import'
    | 'artifact_version_created'
    | 'artifact_revised'
    | 'transformation_recorded'
    | 'approval_requested'
    | 'approval_decided'
    | 'approval_cancelled'
    | 'approval_revoked'
    | 'approval_expired'
    | 'challenge_raised'
    | 'challenge_resolved'
    | 'verification_recorded'
    | 'policy_published'
    | 'actor_registered'
    | 'key_issued'
    | 'key_rotated'
    | 'key_revoked'
    | 'key_compromised'
    | 'accountability_assigned'
    | 'content_redacted'
    | 'content_erased'
    | 'intent_effective_selected'
    | 'intent_merged';
  /**
   * A UUID, RFC 9562. ACT logical identities MUST use UUIDv7 (time-ordered) when newly generated; this pattern accepts any RFC 9562 UUID so that imported/legacy identifiers are not rejected on shape alone.
   */
  export type UUID = string;
  /**
   * A content or event digest in the form algorithm:encoded-value. Version 1.0 requires sha-256.
   */
  export type Digest = string;
  /**
   * A content or event digest in the form algorithm:encoded-value. Version 1.0 requires sha-256.
   */
  export type Digest1 = string;

  /**
   * A DSSE-compatible envelope wrapping a canonical unsigned event payload with one or more signatures. See ACT-1.0.md section 4.2.
   */
  export interface SignedEnvelope {
    payloadType: 'application/vnd.act.event+json';
    payload: UnsignedEvent;
    payloadDigest: Digest1;
    /**
     * @minItems 1
     */
    signatures: [
      {
        key_id: string;
        algorithm: 'ed25519';
        /**
         * base64-encoded Ed25519 signature over the DSSE PAE(payloadType, canonical payload bytes) construction, per https://github.com/secure-systems-lab/dsse/blob/master/protocol.md
         */
        signature: string;
      },
      ...{
        key_id: string;
        algorithm: 'ed25519';
        /**
         * base64-encoded Ed25519 signature over the DSSE PAE(payloadType, canonical payload bytes) construction, per https://github.com/secure-systems-lab/dsse/blob/master/protocol.md
         */
        signature: string;
      }[],
    ];
  }
  /**
   * A reference to the actor and signing key responsible for an event or attestation.
   */
  export interface ActorRef {
    actor_id: UUID;
    /**
     * algorithm:fingerprint, e.g. ed25519:9f86d0...
     */
    key_id: string;
    display_name?: string;
  }
  /**
   * Describes artifact content separately from immutable event metadata, per ACT-1.0.md section 15.
   */
  export interface ContentDescriptor {
    media_type: string;
    byte_length: number;
    digest: Digest;
    storage: {
      kind: 'inline' | 'filesystem' | 'database' | 'external';
      /**
       * base64-encoded content, present only when kind is inline
       */
      inline_value?: string;
      /**
       * opaque storage-provider locator, present when kind is filesystem/database/external
       */
      location?: string;
    };
    encryption?: {
      algorithm: 'none' | 'aes-256-gcm';
      key_id?: string;
      /**
       * base64-encoded nonce, required when algorithm is not none
       */
      nonce?: string;
    };
    sensitivity: 'public' | 'internal' | 'confidential' | 'restricted';
    retention_policy_id?: UUID;
    availability_state: 'available' | 'redacted' | 'erased' | 'unavailable';
  }
}

// source: schemas/event/event-type.schema.json
namespace NS_event_event_type {
  /**
   * The closed enumeration of ACT 1.0 event types. An implementation MUST reject an unrecognized value rather than reinterpret it (ACT-1.0.md section 16.4).
   */
  export type EventType =
    | 'genesis'
    | 'external_import'
    | 'artifact_version_created'
    | 'artifact_revised'
    | 'transformation_recorded'
    | 'approval_requested'
    | 'approval_decided'
    | 'approval_cancelled'
    | 'approval_revoked'
    | 'approval_expired'
    | 'challenge_raised'
    | 'challenge_resolved'
    | 'verification_recorded'
    | 'policy_published'
    | 'actor_registered'
    | 'key_issued'
    | 'key_rotated'
    | 'key_revoked'
    | 'key_compromised'
    | 'accountability_assigned'
    | 'content_redacted'
    | 'content_erased'
    | 'intent_effective_selected'
    | 'intent_merged';
}

// source: schemas/event/ledger-receipt.schema.json
namespace NS_event_ledger_receipt {
  /**
   * A signed, hash-chained receipt a ledger issues when it accepts an event. See ACT-1.0.md section 5.3.
   */
  export type LedgerReceipt = {
    [k: string]: unknown;
  } & {
    ledger_id: UUID;
    sequence: number;
    event_id: Digest;
    accepted_at: string;
    previous_receipt_digest: Digest1;
    receipt_digest: Digest2;
    signature: {
      key_id: string;
      algorithm: 'ed25519';
      signature: string;
    };
  };
  /**
   * A UUID, RFC 9562. ACT logical identities MUST use UUIDv7 (time-ordered) when newly generated; this pattern accepts any RFC 9562 UUID so that imported/legacy identifiers are not rejected on shape alone.
   */
  export type UUID = string;
  /**
   * A content or event digest in the form algorithm:encoded-value. Version 1.0 requires sha-256.
   */
  export type Digest = string;
  /**
   * A content or event digest in the form algorithm:encoded-value. Version 1.0 requires sha-256.
   */
  export type Digest1 = string;
  /**
   * A content or event digest in the form algorithm:encoded-value. Version 1.0 requires sha-256.
   */
  export type Digest2 = string;
}

// source: schemas/event/unsigned-event.schema.json
namespace NS_event_unsigned_event {
  /**
   * The canonical (RFC 8785) unsigned event payload whose SHA-256 digest is the event_id. See ACT-1.0.md section 5.1.
   */
  export type UnsignedEvent = {
    [k: string]: unknown;
  } & {
    protocol_version: 'act/1.0';
    event_type: EventType;
    occurred_at: string;
    actor: ActorRef;
    tenant:
      | string
      | {
          not_applicable: true;
          reason: string;
        };
    subject: {
      kind: 'artifact' | 'transformation' | 'attestation' | 'policy' | 'evidence';
      artifact_id?: UUID;
      version_id?: Digest;
      artifact_type?: string;
    };
    causal_parents: {
      event_id: Digest;
      relation?: 'input' | 'output' | 'revision-of' | 'merge-of' | 'approval-of' | 'response-to';
    }[];
    content_descriptors: ContentDescriptor[];
    policy_context:
      | {
          policy_id: UUID;
          policy_version: Digest;
        }
      | {
          not_applicable: true;
          reason: string;
        };
    payload: {};
    extensions: {};
  };
  /**
   * The closed enumeration of ACT 1.0 event types. An implementation MUST reject an unrecognized value rather than reinterpret it (ACT-1.0.md section 16.4).
   */
  export type EventType =
    | 'genesis'
    | 'external_import'
    | 'artifact_version_created'
    | 'artifact_revised'
    | 'transformation_recorded'
    | 'approval_requested'
    | 'approval_decided'
    | 'approval_cancelled'
    | 'approval_revoked'
    | 'approval_expired'
    | 'challenge_raised'
    | 'challenge_resolved'
    | 'verification_recorded'
    | 'policy_published'
    | 'actor_registered'
    | 'key_issued'
    | 'key_rotated'
    | 'key_revoked'
    | 'key_compromised'
    | 'accountability_assigned'
    | 'content_redacted'
    | 'content_erased'
    | 'intent_effective_selected'
    | 'intent_merged';
  /**
   * A UUID, RFC 9562. ACT logical identities MUST use UUIDv7 (time-ordered) when newly generated; this pattern accepts any RFC 9562 UUID so that imported/legacy identifiers are not rejected on shape alone.
   */
  export type UUID = string;
  /**
   * A content or event digest in the form algorithm:encoded-value. Version 1.0 requires sha-256.
   */
  export type Digest = string;

  /**
   * A reference to the actor and signing key responsible for an event or attestation.
   */
  export interface ActorRef {
    actor_id: UUID;
    /**
     * algorithm:fingerprint, e.g. ed25519:9f86d0...
     */
    key_id: string;
    display_name?: string;
  }
  /**
   * Describes artifact content separately from immutable event metadata, per ACT-1.0.md section 15.
   */
  export interface ContentDescriptor {
    media_type: string;
    byte_length: number;
    digest: Digest;
    storage: {
      kind: 'inline' | 'filesystem' | 'database' | 'external';
      /**
       * base64-encoded content, present only when kind is inline
       */
      inline_value?: string;
      /**
       * opaque storage-provider locator, present when kind is filesystem/database/external
       */
      location?: string;
    };
    encryption?: {
      algorithm: 'none' | 'aes-256-gcm';
      key_id?: string;
      /**
       * base64-encoded nonce, required when algorithm is not none
       */
      nonce?: string;
    };
    sensitivity: 'public' | 'internal' | 'confidential' | 'restricted';
    retention_policy_id?: UUID;
    availability_state: 'available' | 'redacted' | 'erased' | 'unavailable';
  }
}

// source: schemas/federation/bundle.schema.json
namespace NS_federation_bundle {
  /**
   * A content or event digest in the form algorithm:encoded-value. Version 1.0 requires sha-256.
   */
  export type Digest = string;
  /**
   * A UUID, RFC 9562. ACT logical identities MUST use UUIDv7 (time-ordered) when newly generated; this pattern accepts any RFC 9562 UUID so that imported/legacy identifiers are not rejected on shape alone.
   */
  export type UUID = string;
  /**
   * The unsigned event payload, in-band for local/embedded use. Wire transport MAY instead base64-encode this per raw DSSE; both representations MUST hash identically once canonicalized.
   */
  export type UnsignedEvent = {
    [k: string]: unknown;
  } & {
    protocol_version: 'act/1.0';
    event_type: EventType;
    occurred_at: string;
    actor: ActorRef;
    tenant:
      | string
      | {
          not_applicable: true;
          reason: string;
        };
    subject: {
      kind: 'artifact' | 'transformation' | 'attestation' | 'policy' | 'evidence';
      artifact_id?: UUID;
      version_id?: Digest;
      artifact_type?: string;
    };
    causal_parents: {
      event_id: Digest;
      relation?: 'input' | 'output' | 'revision-of' | 'merge-of' | 'approval-of' | 'response-to';
    }[];
    content_descriptors: ContentDescriptor[];
    policy_context:
      | {
          policy_id: UUID;
          policy_version: Digest;
        }
      | {
          not_applicable: true;
          reason: string;
        };
    payload: {};
    extensions: {};
  };
  /**
   * The closed enumeration of ACT 1.0 event types. An implementation MUST reject an unrecognized value rather than reinterpret it (ACT-1.0.md section 16.4).
   */
  export type EventType =
    | 'genesis'
    | 'external_import'
    | 'artifact_version_created'
    | 'artifact_revised'
    | 'transformation_recorded'
    | 'approval_requested'
    | 'approval_decided'
    | 'approval_cancelled'
    | 'approval_revoked'
    | 'approval_expired'
    | 'challenge_raised'
    | 'challenge_resolved'
    | 'verification_recorded'
    | 'policy_published'
    | 'actor_registered'
    | 'key_issued'
    | 'key_rotated'
    | 'key_revoked'
    | 'key_compromised'
    | 'accountability_assigned'
    | 'content_redacted'
    | 'content_erased'
    | 'intent_effective_selected'
    | 'intent_merged';
  /**
   * A content or event digest in the form algorithm:encoded-value. Version 1.0 requires sha-256.
   */
  export type Digest1 = string;
  /**
   * A signed, hash-chained receipt a ledger issues when it accepts an event. See ACT-1.0.md section 5.3.
   */
  export type LedgerReceipt = {
    [k: string]: unknown;
  } & {
    ledger_id: UUID;
    sequence: number;
    event_id: Digest;
    accepted_at: string;
    previous_receipt_digest: Digest2;
    receipt_digest: Digest3;
    signature: {
      key_id: string;
      algorithm: 'ed25519';
      signature: string;
    };
  };
  /**
   * A content or event digest in the form algorithm:encoded-value. Version 1.0 requires sha-256.
   */
  export type Digest2 = string;
  /**
   * A content or event digest in the form algorithm:encoded-value. Version 1.0 requires sha-256.
   */
  export type Digest3 = string;

  /**
   * The unit of export/import between independent ledgers. See spec/federation.md section 2.
   */
  export interface SignedEventBundle {
    bundle_id: Digest;
    source_ledger_id: UUID;
    exported_at: string;
    events: {
      signed_envelope: SignedEnvelope;
      source_receipt: LedgerReceipt;
    }[];
    completeness: {
      scope: 'complete' | 'partial';
      description?: string;
      known_gaps: {
        missing_event_id: Digest;
        referenced_by: Digest;
      }[];
    };
    signature: {
      key_id: string;
      algorithm: 'ed25519';
      signature: string;
    };
  }
  /**
   * A DSSE-compatible envelope wrapping a canonical unsigned event payload with one or more signatures. See ACT-1.0.md section 4.2.
   */
  export interface SignedEnvelope {
    payloadType: 'application/vnd.act.event+json';
    payload: UnsignedEvent;
    payloadDigest: Digest1;
    /**
     * @minItems 1
     */
    signatures: [
      {
        key_id: string;
        algorithm: 'ed25519';
        /**
         * base64-encoded Ed25519 signature over the DSSE PAE(payloadType, canonical payload bytes) construction, per https://github.com/secure-systems-lab/dsse/blob/master/protocol.md
         */
        signature: string;
      },
      ...{
        key_id: string;
        algorithm: 'ed25519';
        /**
         * base64-encoded Ed25519 signature over the DSSE PAE(payloadType, canonical payload bytes) construction, per https://github.com/secure-systems-lab/dsse/blob/master/protocol.md
         */
        signature: string;
      }[],
    ];
  }
  /**
   * A reference to the actor and signing key responsible for an event or attestation.
   */
  export interface ActorRef {
    actor_id: UUID;
    /**
     * algorithm:fingerprint, e.g. ed25519:9f86d0...
     */
    key_id: string;
    display_name?: string;
  }
  /**
   * Describes artifact content separately from immutable event metadata, per ACT-1.0.md section 15.
   */
  export interface ContentDescriptor {
    media_type: string;
    byte_length: number;
    digest: Digest;
    storage: {
      kind: 'inline' | 'filesystem' | 'database' | 'external';
      /**
       * base64-encoded content, present only when kind is inline
       */
      inline_value?: string;
      /**
       * opaque storage-provider locator, present when kind is filesystem/database/external
       */
      location?: string;
    };
    encryption?: {
      algorithm: 'none' | 'aes-256-gcm';
      key_id?: string;
      /**
       * base64-encoded nonce, required when algorithm is not none
       */
      nonce?: string;
    };
    sensitivity: 'public' | 'internal' | 'confidential' | 'restricted';
    retention_policy_id?: UUID;
    availability_state: 'available' | 'redacted' | 'erased' | 'unavailable';
  }
}

// source: schemas/policy/authority-policy.schema.json
namespace NS_policy_authority_policy {
  /**
   * A UUID, RFC 9562. ACT logical identities MUST use UUIDv7 (time-ordered) when newly generated; this pattern accepts any RFC 9562 UUID so that imported/legacy identifiers are not rejected on shape alone.
   */
  export type UUID = string;
  /**
   * A content or event digest in the form algorithm:encoded-value. Version 1.0 requires sha-256.
   */
  export type Digest = string;

  /**
   * Defines who may select, merge, or supersede an effective Intent for a project/branch, and whether quorum or separation of duties applies. See ACT-1.0.md section 7.3.
   */
  export interface AuthorityPolicy {
    policy_id: UUID;
    policy_version: Digest;
    scope: {
      project: string;
      branch?: string;
    };
    /**
     * @minItems 1
     */
    authorized_roles: [string, ...string[]];
    quorum: number;
    separation_of_duties?: boolean;
    issued_by: ActorRef;
    issued_at: string;
    /**
     * @minItems 1
     */
    signatures: [
      {
        key_id: string;
        algorithm: 'ed25519';
        signature: string;
      },
      ...{
        key_id: string;
        algorithm: 'ed25519';
        signature: string;
      }[],
    ];
  }
  /**
   * A reference to the actor and signing key responsible for an event or attestation.
   */
  export interface ActorRef {
    actor_id: UUID;
    /**
     * algorithm:fingerprint, e.g. ed25519:9f86d0...
     */
    key_id: string;
    display_name?: string;
  }
}

// source: schemas/policy/policy.schema.json
namespace NS_policy_policy {
  /**
   * A UUID, RFC 9562. ACT logical identities MUST use UUIDv7 (time-ordered) when newly generated; this pattern accepts any RFC 9562 UUID so that imported/legacy identifiers are not rejected on shape alone.
   */
  export type UUID = string;
  /**
   * A content or event digest in the form algorithm:encoded-value. Version 1.0 requires sha-256.
   */
  export type Digest = string;

  /**
   * A versioned, immutable policy document governing authorization, approval, retention, verification, or trust. See ACT-1.0.md section 12.
   */
  export interface Policy {
    policy_id: UUID;
    policy_version: Digest;
    kind: 'authorization' | 'approval' | 'retention' | 'verification' | 'trust' | 'authority';
    name: string;
    description?: string;
    issued_by: ActorRef;
    issued_at: string;
    supersedes?: Digest | null;
    /**
     * @minItems 1
     */
    rules: [
      {
        rule_id: string;
        /**
         * A declarative match predicate over subject type, semantic_change_claim.classification, artifact_type, sensitivity, or tenant.
         */
        when: {
          subject_kind?: string[];
          artifact_type?: string[];
          semantic_change_classification?: string[];
          sensitivity?: string[];
        };
        require: {
          approval?: boolean;
          quorum?: number;
          reviewer_roles?: string[];
          /**
           * If true, the reviewer MUST NOT be the same actor as the transformation's author.
           */
          separation_of_duties?: boolean;
        };
      },
      ...{
        rule_id: string;
        /**
         * A declarative match predicate over subject type, semantic_change_claim.classification, artifact_type, sensitivity, or tenant.
         */
        when: {
          subject_kind?: string[];
          artifact_type?: string[];
          semantic_change_classification?: string[];
          sensitivity?: string[];
        };
        require: {
          approval?: boolean;
          quorum?: number;
          reviewer_roles?: string[];
          /**
           * If true, the reviewer MUST NOT be the same actor as the transformation's author.
           */
          separation_of_duties?: boolean;
        };
      }[],
    ];
    /**
     * @minItems 1
     */
    signatures: [
      {
        key_id: string;
        algorithm: 'ed25519';
        signature: string;
      },
      ...{
        key_id: string;
        algorithm: 'ed25519';
        signature: string;
      }[],
    ];
  }
  /**
   * A reference to the actor and signing key responsible for an event or attestation.
   */
  export interface ActorRef {
    actor_id: UUID;
    /**
     * algorithm:fingerprint, e.g. ed25519:9f86d0...
     */
    key_id: string;
    display_name?: string;
  }
}

export type ApprovalDecision = NS_approval_approval_decision.ApprovalDecision;
export type ApprovalRequest = NS_approval_approval_request.ApprovalRequest;
export type ArtifactEnvelope = NS_artifact_artifact_envelope.ArtifactEnvelope;
export type Transformation = NS_artifact_transformation.Transformation;
export type AccountabilityAssignment =
  NS_artifact_types_accountability_assignment.AccountabilityAssignment;
export type Actor = NS_artifact_types_actor.Actor;
export type AIProposal = NS_artifact_types_ai_proposal.AIProposal;
export type Ambiguity = NS_artifact_types_ambiguity.Ambiguity;
export type ApprovalDecisionArtifact = NS_artifact_types_approval_decision.ApprovalDecisionArtifact;
export type ApprovalRequestArtifact = NS_artifact_types_approval_request.ApprovalRequestArtifact;
export type Architecture = NS_artifact_types_architecture.Architecture;
export type Assumption = NS_artifact_types_assumption.Assumption;
export type ChallengeArtifact = NS_artifact_types_challenge.ChallengeArtifact;
export type Constraint = NS_artifact_types_constraint.Constraint;
export type Decision = NS_artifact_types_decision.Decision;
export type Evidence = NS_artifact_types_evidence.Evidence;
export type Goal = NS_artifact_types_goal.Goal;
export type HumanProposal = NS_artifact_types_human_proposal.HumanProposal;
export type Intent = NS_artifact_types_intent.Intent;
export type Key = NS_artifact_types_key.Key;
export type PolicyArtifact = NS_artifact_types_policy.PolicyArtifact;
export type Prompt = NS_artifact_types_prompt.Prompt;
export type Requirement = NS_artifact_types_requirement.Requirement;
export type Revision = NS_artifact_types_revision.Revision;
export type Risk = NS_artifact_types_risk.Risk;
export type RuntimeObservation = NS_artifact_types_runtime_observation.RuntimeObservation;
export type SourceCode = NS_artifact_types_source_code.SourceCode;
export type Task = NS_artifact_types_task.Task;
export type Test = NS_artifact_types_test.Test;
export type ToolInvocation = NS_artifact_types_tool_invocation.ToolInvocation;
export type UserFeedback = NS_artifact_types_user_feedback.UserFeedback;
export type VerificationReport = NS_artifact_types_verification_report.VerificationReport;
export type Challenge = NS_challenge_challenge.Challenge;
export type ActorRef = NS_common_actor_ref.ActorRef;
export type ConfidenceAssessment = NS_common_confidence_assessment.ConfidenceAssessment;
export type ContentDescriptor = NS_common_content_descriptor.ContentDescriptor;
export type Digest = NS_common_digest.Digest;
export type Uncertainty = NS_common_uncertainty.Uncertainty;
export type UUID = NS_common_uuid.UUID;
export type SignedEnvelope = NS_envelope_signed_envelope.SignedEnvelope;
export type EventType = NS_event_event_type.EventType;
export type LedgerReceipt = NS_event_ledger_receipt.LedgerReceipt;
export type UnsignedEvent = NS_event_unsigned_event.UnsignedEvent;
export type SignedEventBundle = NS_federation_bundle.SignedEventBundle;
export type AuthorityPolicy = NS_policy_authority_policy.AuthorityPolicy;
export type Policy = NS_policy_policy.Policy;
